import { Text, View } from 'react-native';

export default function Index() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text style={{ fontSize: 20, fontWeight: '600' }}>BalanceNote — Mobile</Text>
      <Text style={{ marginTop: 8, color: '#666' }}>Story 1.1 부트스트랩 placeholder</Text>
    </View>
  );
}
