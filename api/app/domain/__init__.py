"""도메인 모듈 — 비즈니스 규칙·텍스트 SOT.

본 디렉토리는 Story 1.3에서 처음 도입(`legal_documents.py`).
Story 1.5+(`allergens.py`), Story 3.x(`kdris.py`, `fit_score.py`, `bmr.py`,
`ad_expression_guard.py`) 등이 후속으로 추가한다. re-export 패턴 anti-pattern —
``__init__.py``는 빈 모듈로 유지.
"""
