# Story 1.1: 프로젝트 부트스트랩 (Docker 1-cmd + 풀스택 골격 + CI 게이트)

Status: ready-for-dev

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a **외주 클라이언트 개발자(인수인계 대상)**,
I want **git clone 후 `docker compose up` 1-cmd로 풀스택 환경(Postgres+pgvector+Redis+FastAPI+seed)이 부팅되고 PR 시 ruff·mypy·pytest·tsc·openapi diff 게이트가 자동 작동하기를**,
So that **README + 8시간 내 로컬 환경 구축 + 자체 데이터 통합이 가능한 인수인계 가능 산출물의 토대가 박힌다.**

## Acceptance Criteria

> **BDD 형식 — 각 AC는 독립적 검증 가능. 모든 AC 통과 후 `Status: ready-for-review`로 전환.**

1. **AC1 — Docker 1-cmd 부팅**
   **Given** 빈 디렉토리 + 본 repo clone, **When** `docker compose up`,
   **Then** `postgres`(pgvector pg17) + `redis` + `api`(FastAPI) + `seed`(one-shot) 컨테이너가 일괄 부팅되고 `api` 컨테이너 healthcheck 통과까지 ≤ 10분.
   - `api`는 `postgres`/`redis` healthy 후에만 시작 (depends_on `condition: service_healthy`).
   - `seed` 컨테이너는 `postgres` healthy 후 1회 실행 후 종료(`restart: no`).

2. **AC2 — Web 부트스트랩 (Next.js 16.2.4)**
   `pnpm dev` 실행 시 `localhost:3000` 응답. App Router + Tailwind + TS + Turbopack + `src/` + import alias `@/*` 모두 starter 디폴트로 적용.
   - `web/AGENTS.md` 자동 생성됨(create-next-app 16 기능).

3. **AC3 — Mobile 부트스트랩 (Expo SDK 54)**
   `pnpm start`(또는 `npx expo start`) 시 Expo dev menu가 동작하고 빈 라우팅 화면(Expo Router file-based, `app/index.tsx`) 응답.

4. **AC4 — API 헬스체크 + Swagger**
   `localhost:8000`에서 다음 엔드포인트 모두 200 응답:
   - `GET /healthz` — liveness, DB ping (asyncpg `SELECT 1`)
   - `GET /readyz` — readiness, Postgres + Redis + pgvector extension 존재 확인 (`SELECT extname FROM pg_extension WHERE extname='vector'`)
   - `GET /docs` — Swagger UI (FastAPI 자동 생성)
   - `GET /openapi.json` — OpenAPI schema

5. **AC5 — 관측성 + Rate limit 골격**
   다음 SDK·미들웨어가 `/api/app/main.py` 부팅 시 초기화 완료(설치만, 정교화는 Epic 8):
   - `sentry-sdk`(Python) — DSN env 설정 시 활성, `before_send` hook이 `app/core/sentry.py`에 등록되어 민감정보 마스킹 자리표시자(`mask_event(event)`) 동작.
   - `structlog` — JSON renderer + `mask_processor`(자리표시자) 등록 + 표준 필드(`timestamp`, `level`, `request_id`, `event`).
   - `slowapi` + Redis backend — 분당 60 / 시간당 1000 / LangGraph 분당 10 3-tier 키 정의 1개 등록(라우터 적용은 후속 스토리).
   - `request_id` 미들웨어 — UUIDv4 생성 + 응답 헤더 `X-Request-ID`.

6. **AC6 — 환경 변수 + secret 표준화**
   - 루트 `.env.example`에 모든 필수 환경변수 키 + 1줄 설명 + 권장 값(또는 placeholder) 기재.
   - `.env`, `.env.local`은 `.gitignore`에 등재.
   - 최소 변수: `DATABASE_URL`, `REDIS_URL`, `JWT_USER_SECRET`, `JWT_ADMIN_SECRET`, `JWT_USER_ISSUER`, `JWT_ADMIN_ISSUER`, `OPENAI_API_KEY`, `ANTHROPIC_API_KEY`, `LANGSMITH_API_KEY`, `LANGSMITH_PROJECT`, `LANGCHAIN_TRACING_V2`, `SENTRY_DSN`, `GOOGLE_OAUTH_CLIENT_ID`, `GOOGLE_OAUTH_CLIENT_SECRET`, `R2_ACCOUNT_ID`, `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`, `R2_BUCKET`, `R2_PUBLIC_BASE_URL`, `EXPO_ACCESS_TOKEN`, `MFDS_OPENAPI_KEY`, `TOSS_SECRET_KEY`(또는 `STRIPE_SECRET_KEY`).

7. **AC7 — CI 게이트 (`.github/workflows/ci.yml`)**
   PR 트리거 시 다음 잡 모두 실행 + 1개라도 실패하면 머지 차단:
   - `lint-format-api` — `ruff check` + `ruff format --check` (api)
   - `typecheck-api` — `mypy app/` (api)
   - `test-api` — `pytest` + coverage ≥ 70% 미달 시 fail (현재 0개 테스트 통과 OK, 임계는 향후 측정 — *최소 1개 smoke test 통과 + coverage gate 활성화*)
   - `typecheck-web` — `pnpm tsc --noEmit` (web)
   - `typecheck-mobile` — `pnpm tsc --noEmit` (mobile)
   - `openapi-diff` — `gen_openapi_clients.sh` 결과 git diff 발생 시 fail (PR에서 schema 동기 강제)
   - `alembic-dry-run` — `alembic upgrade head --sql`을 PR 코멘트로 표시 (게시는 fail 비차단, 단순 표시)

8. **AC8 — main 머지 자동 배포 + 마이그레이션**
   - main 브랜치 머지 시 Vercel(Web) + Railway(API) 자동 배포가 *연결*되어 있다(실제 배포 영구 검증은 W7, 본 스토리는 *연결 + secret 등록 + README 명세*).
   - Railway 배포 hook 또는 별 GitHub Actions 잡으로 `alembic upgrade head` 자동 실행 (현재는 빈 마이그레이션 또는 `users` placeholder).

9. **AC9 — 표준 메타파일 커밋**
   다음 파일 모두 커밋(빈 placeholder 허용, 단 향후 스토리에서 채워질 자리표시자 명시):
   - 루트: `.gitignore`, `.editorconfig`, `AGENTS.md`(외주 인수 컨텍스트), `docker-compose.yml`, `.env.example`, `README.md`(향후 5섹션 placeholder)
   - `api/`: `pyproject.toml`(ruff/mypy/pytest 룰셋), `uv.lock`, `.python-version`, `alembic.ini`, `app/main.py`, `app/core/{config,security,logging,sentry,middleware,exceptions}.py`(placeholder)
   - `web/`: starter 결과물 + `AGENTS.md` 자동 생성
   - `mobile/`: starter 결과물 + `AGENTS.md`(수동 추가)
   - `docker/`: `Dockerfile.api`, `postgres-init/01-pgvector.sql`(`CREATE EXTENSION IF NOT EXISTS vector`)
   - `.github/workflows/ci.yml`

10. **AC10 — `pnpm gen:api` 스크립트 동작**
    백엔드 가동 상태에서 `pnpm gen:api`(루트 또는 `web` 실행) 시 `web/src/lib/api-client.ts`(생성물)와 `mobile/lib/api-client.ts`가 OpenAPI 스키마로부터 자동 생성된다.
    - `scripts/gen_openapi_clients.sh` 래퍼 + `openapi-typescript` 의존성.

## Tasks / Subtasks

### Task 1 — 프로젝트 폴더 부트스트랩 (AC: #1, #9)

- [ ] 1.1 루트 디렉토리에 `.gitignore`(Python + Node + Expo + .env + .DS_Store + IDE), `.editorconfig`(LF + utf-8 + indent), `AGENTS.md`, 빈 `docker-compose.yml`, `.env.example`(AC6 변수 전체) 작성. `AGENTS.md`는 외주 인수 컨텍스트 1페이지 — (a) 프로젝트 1줄 요약(BalanceNote), (b) 단일 repo + multi-folder 결정, (c) 컴포넌트 경계 = 폴더 = 컨테이너, (d) `_bmad-output/` 산출물 위치(prd/architecture/epics/스토리), (e) `pnpm gen:api` 명령 1줄 강조.
- [ ] 1.2 `_bmad-output/`은 `.gitignore`에서 제외(본 산출물 트래킹 유지)
- [ ] 1.3 README.md를 5섹션 placeholder로 갱신 (Quick start / Architecture / 데이터 모델 / 영업 closing FAQ / 클라우드 마이그레이션) — 본 스토리는 Quick start 섹션만 채움, 나머지는 Epic 8 (Story 8.3)

### Task 2 — Web 부트스트랩 (AC: #2, #9)

- [ ] 2.1 `pnpm create next-app@latest web --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --turbopack`
- [ ] 2.2 `web/AGENTS.md` 생성 확인 + 본 프로젝트 컨텍스트(피드백 톤·디스클레이머·도메인 enum 위치) 1단락 추가
- [ ] 2.3 `web/.env.example` 작성 (백엔드 base URL, public OAuth client id 등)
- [ ] 2.4 `web/src/app/page.tsx`에 *"BalanceNote — Web"* 1줄 placeholder

### Task 3 — Mobile 부트스트랩 (AC: #3, #9)

- [ ] 3.1 `npx create-expo-app@latest mobile --template default`
- [ ] 3.2 starter가 `package-lock.json`을 생성한 경우 삭제 후 `cd mobile && pnpm install`로 `pnpm-lock.yaml` 채택 (TS 측 패키지 매니저 통일)
- [ ] 3.3 `mobile/AGENTS.md` 수동 작성 1단락 (Expo Router file-based + 권한 흐름 표준 컴포넌트 위치 = `mobile/lib/permissions.ts`/`features/onboarding/PermissionDeniedView.tsx` 예고)
- [ ] 3.4 `mobile/.env.example` 작성
- [ ] 3.5 `mobile/app/index.tsx` placeholder *"BalanceNote — Mobile"* 1줄

### Task 4 — API 부트스트랩 (AC: #4, #5, #9)

- [ ] 4.1 `cd api && uv init --python 3.13`
- [ ] 4.2 의존성 추가:
  - 핵심: `uv add 'fastapi[standard]' langgraph langchain langchain-openai langchain-anthropic`
  - DB: `uv add 'sqlalchemy[asyncio]' alembic asyncpg pgvector pydantic-settings`
  - 인프라: `uv add redis httpx tenacity sentry-sdk 'python-jose[cryptography]' passlib slowapi apscheduler langgraph-checkpoint-postgres sse-starlette`
  - 옵저버빌리티: `uv add langsmith structlog`
  - 개발: `uv add --dev pytest pytest-asyncio pytest-cov ruff mypy`
- [ ] 4.3 `api/pyproject.toml`에 ruff 룰셋(`select = ["E","F","W","I","UP","B","SIM","ASYNC"]` + `line-length = 100`), mypy 옵션(`strict = true`), pytest 옵션(`--cov=app --cov-report=term --cov-fail-under=70`) 추가
- [ ] 4.4 `api/app/main.py` — FastAPI app 인스턴스 + `/healthz` + `/readyz` + Sentry init + structlog init + slowapi limiter 등록 + `request_id` 미들웨어
- [ ] 4.5 `api/app/core/config.py` — `pydantic-settings` `BaseSettings`로 `.env` 로딩, AC6 변수 모두 typed field
- [ ] 4.6 `api/app/core/sentry.py` — `mask_event(event, hint)` 자리표시자(향후 NFR-S5 정규식 추가) + `before_send` 등록
- [ ] 4.7 `api/app/core/logging.py` — `structlog.configure` + `mask_processor` 자리표시자 + JSON renderer (로컬 dev는 console renderer)
- [ ] 4.8 `api/app/core/middleware.py` — `request_id` 미들웨어 (UUIDv4 + 응답 헤더 `X-Request-ID` + structlog `bind_contextvars`)
- [ ] 4.9 `api/app/core/security.py`, `exceptions.py` — placeholder만(Epic 1 후속 스토리에서 채움)
- [ ] 4.10 `api/alembic.ini` + `api/alembic/env.py` — async engine 호출 형식 + `target_metadata = None`(향후 모델 추가 시 갱신)

### Task 5 — Docker Compose 1-cmd (AC: #1, #9)

- [ ] 5.1 `docker/Dockerfile.api` — `python:3.13-slim` 기반, `uv` 설치, `uv sync`, `uvicorn app.main:app --host 0.0.0.0 --port 8000` 실행
- [ ] 5.2 `docker/postgres-init/01-pgvector.sql` — `CREATE EXTENSION IF NOT EXISTS vector;` + `CREATE SCHEMA IF NOT EXISTS lg_checkpoints;`
- [ ] 5.3 `docker-compose.yml` 작성 — 서비스 4종:
  - `postgres`: image `pgvector/pgvector:pg17`, volume mount `./docker/postgres-init:/docker-entrypoint-initdb.d`, healthcheck `pg_isready`
  - `redis`: `redis:7-alpine`, healthcheck `redis-cli ping`
  - `api`: build `./docker/Dockerfile.api`, depends_on postgres+redis `service_healthy`, env_file `.env`, volume mount `./api:/app`(dev), healthcheck `curl /healthz`
  - `seed`: 동일 build, command `python -m scripts.bootstrap_seed`, depends_on postgres `service_healthy`, restart `no` (본 스토리에서는 placeholder no-op 스크립트)
- [ ] 5.4 `scripts/bootstrap_seed.py` placeholder — 향후 `seed_food_db.py` + `seed_guidelines.py` 호출 entrypoint

### Task 6 — Alembic 초기 마이그레이션 (AC: #4, #8)

- [ ] 6.1 `alembic init alembic` 후 `env.py`를 async용으로 수정 (`run_migrations_online`을 async engine으로)
- [ ] 6.2 빈 마이그레이션 1개 생성 (`alembic revision -m "0001_baseline"`) — 본 스토리 종료 시점에 `users` 테이블 마이그레이션은 Story 1.2/1.5 영역. 단, `lg_checkpoints` 스키마 생성 SQL은 `01-pgvector.sql`에 박았으므로 alembic은 baseline만.
- [ ] 6.3 `alembic upgrade head` 컨테이너 부팅 시 자동 실행되도록 `Dockerfile.api` entrypoint에 `alembic upgrade head && uvicorn ...` 체이닝

### Task 7 — CI 게이트 (AC: #7, #8)

- [ ] 7.1 `.github/workflows/ci.yml` — PR 트리거 + 잡 7종(AC7) 정의
  - 잡별 service 컨테이너로 postgres + redis 사용 (test-api 잡)
  - mobile 잡은 Expo CLI 의존성 캐싱
  - openapi-diff 잡은 api 잡 가동 후 스키마 추출 → 기존 `web/src/lib/api-client.ts`와 `git diff` 비교
- [ ] 7.2 main 브랜치 push 시 별 잡 `deploy.yml`(또는 동일 워크플로우 분기) — Vercel/Railway는 GitHub 연동으로 자동(secret 등록만), `alembic upgrade head`는 Railway 배포 hook 또는 별 잡으로 처리
- [ ] 7.3 `.github/workflows/daily-cost-alert.yml` placeholder (cron `0 0 * * *` UTC = 09:00 KST, 본 스토리는 빈 step만 — Epic 8 Story 8.5에서 구현)
- [ ] 7.4 README.md Quick start 섹션 + Vercel/Railway secret 등록 SOP 1단락

### Task 8 — OpenAPI 클라이언트 생성 스크립트 (AC: #10)

- [ ] 8.1 `scripts/gen_openapi_clients.sh` — `curl http://localhost:8000/openapi.json -o /tmp/openapi.json` + `pnpm exec openapi-typescript /tmp/openapi.json -o web/src/lib/api-client.ts` + `mobile/lib/api-client.ts`
- [ ] 8.2 `web/package.json`, `mobile/package.json`에 `"gen:api": "../scripts/gen_openapi_clients.sh"` 스크립트 추가
- [ ] 8.3 `web`/`mobile`에 `openapi-typescript` devDependency 추가

### Task 9 — 검증 & 문서화 (AC: #1-#10)

- [ ] 9.1 `docker compose up` 1-cmd 실행 후 4개 컨테이너 healthy 확인 + `/healthz` `/readyz` `/docs` 200 응답 수동 검증
- [ ] 9.2 `pnpm dev`(web), `pnpm start`(mobile) 각각 동작 확인
- [ ] 9.3 PR 1개 생성하여 CI 7잡 모두 green 확인 (smoke commit)
- [ ] 9.4 README.md Quick start 섹션 — 위 3개 동작 명령 명세 + 8시간 onboarding 체크리스트
- [ ] 9.5 본 파일 하단 Dev Agent Record에 File List + Completion Notes 작성 후 Status: ready-for-review

## Dev Notes

### 핵심 Architecture 결정 (반드시 따를 것)

**Repo 구조 — 단일 repo + multi-folder, 워크스페이스 X**
[Source: architecture.md#Repo 구조 결정]
- pnpm workspace / Turborepo / Nx **모두 금지**. Python(api) ↔ TS(mobile/web) 워크스페이스 통합 불가 — 반쪽짜리 monorepo는 안티패턴.
- 컴포넌트 경계 = 폴더 = 컨테이너 (Docker Compose 1-cmd와 정합).
- TS 타입 공유는 OpenAPI 단일 출처 → `openapi-typescript` 자동 생성 (`pnpm gen:api`).

**버전 핀 (절대 임의 변경 금지)**
[Source: architecture.md#Component-별 Starter, #Data Architecture]
- Postgres **17** + `pgvector/pgvector:pg17` 이미지 + pgvector **0.8.2**
  - ⚠ architecture.md 본문 일부에 `pg16`이 한 곳 남아있으나(line 216), 데이터 결정 표(line 285)와 epics.md AC가 모두 **pg17**로 일치 — pg17 사용.
- Python **3.13**, FastAPI(standard), LangGraph **1.1.9**, SQLAlchemy **2.0.49** async + asyncpg
- Next.js **16.2.4** (App Router + Tailwind v3 + Turbopack)
- Expo **SDK 54**
- 패키지 매니저: **uv**(Python), **pnpm**(TS) — pip/poetry/npm/yarn 금지

**`lg_checkpoints` 스키마는 Alembic 외부**
[Source: architecture.md#Decision Impact Analysis "Cross-Component Dependencies"]
- LangGraph `AsyncPostgresSaver`는 자체 `.setup()`으로 스키마 관리 — Alembic이 추적 X.
- 본 스토리에서는 `01-pgvector.sql`에 `CREATE SCHEMA IF NOT EXISTS lg_checkpoints;`만 박고, 테이블 생성은 Story 3.3에서 `AsyncPostgresSaver(...).setup()` 호출로 처리.

**API 응답 표준 (전체 횡단)**
[Source: architecture.md#API & Communication Patterns, #Format Patterns]
- 성공: 직접 객체 (wrapper 금지). `{"id": ..., ...}`
- 에러: **RFC 7807 Problem Details** — `application/problem+json` + `type/title/status/detail/instance` + 우리 확장 `code`
- 응답·요청 wire format **snake_case 양방향**. Pydantic `alias_generator=to_camel` 도입 금지.
- URL: REST kebab-plural noun + `/v1/` (예: `/v1/audit-logs`). 본 스토리는 health 엔드포인트만이라 `/healthz`, `/readyz`는 예외 (운영 표준).

**환경 분리**
[Source: architecture.md#Infrastructure & Deployment]
- dev: Docker Compose 로컬 (본 스토리 검증 대상)
- staging: Railway preview env (PR push 자동) — 본 스토리는 secret 등록 + 연결만
- prod: Railway prod (main push 자동)
- Web은 Vercel — main push 시 자동.
- secret은 **Railway secrets / Vercel env**에서 관리. `.env.example`은 변수명+설명만, 실제 값 X.

### 폴더 구조 (Story 1.1 종료 시점에 *반드시* 존재해야 하는 골격)

[Source: architecture.md#Complete Project Directory Structure (line 569-841)]

```
AI_diet/
├── README.md                        # 5섹션 placeholder (본 스토리: Quick start만 채움)
├── AGENTS.md                        # 외주 인수 컨텍스트 1페이지
├── .env.example                     # AC6 모든 변수
├── .gitignore                       # Python + Node + Expo + .env + IDE
├── .editorconfig
├── docker-compose.yml               # postgres + redis + api + seed
├── .github/workflows/
│   ├── ci.yml                       # PR 게이트 7잡
│   └── daily-cost-alert.yml         # placeholder (Epic 8)
├── docker/
│   ├── Dockerfile.api
│   └── postgres-init/01-pgvector.sql
├── scripts/
│   ├── bootstrap_seed.py            # placeholder (Story 3.1/3.2가 채움)
│   └── gen_openapi_clients.sh
├── docs/                            # SOP/FAQ/Runbook 폴더만 (Epic 8이 채움)
├── api/
│   ├── pyproject.toml
│   ├── uv.lock
│   ├── .python-version              # 3.13
│   ├── alembic.ini
│   ├── alembic/
│   │   ├── env.py                   # async engine
│   │   └── versions/0001_baseline.py
│   ├── app/
│   │   ├── __init__.py
│   │   ├── main.py                  # FastAPI app + /healthz + /readyz + 미들웨어/SDK 초기화
│   │   └── core/
│   │       ├── __init__.py
│   │       ├── config.py            # pydantic-settings BaseSettings
│   │       ├── security.py          # placeholder (Story 1.2)
│   │       ├── logging.py           # structlog + mask_processor 자리표시자
│   │       ├── sentry.py            # before_send + mask_event 자리표시자
│   │       ├── middleware.py        # request_id 미들웨어
│   │       └── exceptions.py        # placeholder (RFC 7807 핸들러는 Story 1.2+)
│   └── tests/
│       ├── __init__.py
│       ├── conftest.py              # async test client fixture
│       └── test_health.py           # /healthz /readyz smoke
├── web/                             # create-next-app 16 결과물
│   └── src/app/page.tsx             # placeholder
├── mobile/                          # create-expo-app 결과물
│   └── app/index.tsx                # placeholder
└── _bmad-output/                    # .gitignore 제외
```

**중요:** 위에서 placeholder로 표시된 모듈/도메인 폴더(`app/domain/`, `app/graph/`, `app/rag/`, `app/api/`, `app/db/`, `app/adapters/`, `app/services/`, `app/workers/`)는 본 스토리에서는 *생성하지 않는다*. 후속 스토리가 도입할 때 함께 만든다 (premature scaffolding 회피).

### 라이브러리·프레임워크 요구사항

| 영역 | 라이브러리 | 정확한 사유 |
|------|-----------|-----------|
| Python 패키지 매니저 | `uv` | pip/poetry 대비 10x + lock 파일 표준 [arch#Component-별 Starter] |
| Web framework | `fastapi[standard]` | starter 의존성 묶음 + `uvicorn[standard]` 포함 |
| ORM | SQLAlchemy 2.0.49 async + asyncpg | LangGraph async 노드 정합. SQLModel 금지(jsonb 키 정밀 X) |
| Migration | Alembic — autogenerate + 수동 review | PR review에서 destructive 차단 |
| Vector DB | pgvector 0.8.2 (HNSW) | Postgres extension. Pinecone/Weaviate 외부 의존 회피 |
| Settings | `pydantic-settings` | `.env` 로딩 표준 |
| Observability (LLM) | `langsmith` SDK + `LANGCHAIN_TRACING_V2` env | LangGraph native 통합 — 별 wrapping 0 |
| Error tracking | `sentry-sdk` (Python) | `before_send` hook 자리표시자만 본 스토리 |
| Logging | `structlog` JSON renderer | NFR-S5 마스킹 processor 자리표시자만 본 스토리 |
| Rate limit | `slowapi` + Redis backend | 분당/시간당/LangGraph 별도 3-tier |
| HTTP client | `httpx` | 식약처 OpenAPI / OpenAI / Anthropic 모두 사용 |
| Retry | `tenacity` | LLM fallback (NFR-R2) |
| Auth | `python-jose[cryptography]` + `passlib` | JWT user/admin 분리 (Story 1.2) |
| SSE | `sse-starlette` | FR26 (Story 3.7) |
| Cron | `apscheduler` | in-process AsyncIOScheduler (FR29 Story 4.2) |
| LangGraph 영속성 | `langgraph-checkpoint-postgres` | AsyncPostgresSaver (Story 3.3) |
| OpenAPI 클라이언트 생성 | `openapi-typescript` (devDependency in web/mobile) | `pnpm gen:api` |
| Web UI lib | shadcn/ui (선택적) + Tailwind v3 | 풀 디자인 시스템 도입 회피. 본 스토리는 starter 디폴트만 |
| Web 차트 | recharts | Story 4.3에서 도입 — 본 스토리에서는 미설치 |
| Web auth | next-auth **금지** — 백엔드 JWT를 httpOnly 쿠키로 직접 교환 | [arch#Frontend Architecture] 명시 |
| Mobile router | Expo Router (file-based) | starter 디폴트 |
| Mobile state | TanStack Query (server) + useState/useReducer (client) | Redux/Jotai/Recoil 도입 금지 [arch#Frontend Architecture] |
| Mobile form | `react-hook-form` + `zod` | Story 1.5에서 도입 — 본 스토리는 미설치 |

### 명명 규칙 (전체 횡단)

[Source: architecture.md#Naming Patterns]
- DB: snake_case + 복수 (`users`, `meals`). FK: `<singular>_id`. 인덱스: `idx_<table>_<cols>`. Enum: `<table>_<col>_enum`.
- API URL: REST kebab-plural + `/v1/` (예: `GET /v1/audit-logs`). Query: snake_case.
- JSON wire: **snake_case 양방향** (Pydantic alias 금지).
- Python: snake_case / PascalCase / UPPER_SNAKE.
- TS: camelCase / PascalCase / UPPER_SNAKE.
- 파일명: Python `.py` snake_case / React `.tsx` PascalCase / 기타 TS kebab-case / Next.js·Expo Router 폴더 kebab-case.

### 테스트 요구사항

[Source: architecture.md#Structure Patterns "테스트 위치", PRD NFR-M1]
- 백엔드 테스트 위치: `api/tests/` 미러 구조 (`tests/test_health.py`, `tests/graph/test_evaluate_fit.py` 향후).
- 프런트엔드 테스트 위치: co-located (`features/meals/MealCard.test.tsx`).
- 본 스토리 *최소 테스트*:
  - `api/tests/test_health.py` — `httpx.AsyncClient`로 `/healthz`, `/readyz` 200 + JSON 응답 본문 검증 (smoke).
  - `api/tests/conftest.py` — `pytest-asyncio` async fixture + 테스트 DB(별 testdb 또는 in-memory `sqlite+aiosqlite` *불가, pgvector 필수* — 컨테이너 service로 PG 사용).
- coverage gate: pyproject.toml `[tool.pytest.ini_options]`에 `addopts = "--cov=app.core --cov-fail-under=70"` (NFR-M1 *핵심 경로* 한정). `app/main.py`, placeholder 모듈은 `[tool.coverage.run] omit`로 제외. 본 스토리 종료 시점에는 `app.core.config`, `app.core.middleware`, `app.core.logging`, `app.core.sentry`만 측정 대상이며 health smoke 테스트로 70% 가능. 후속 스토리가 도메인 코드를 추가할 때 `--cov=app.core,app.domain,app.graph,app.services` 등으로 확장.
- TS 측은 본 스토리에서 단위 테스트 X — `tsc --noEmit` 게이트만.

### CI 게이트 세부 (`.github/workflows/ci.yml`)

[Source: architecture.md#Infrastructure & Deployment "배포 게이트", epics.md#Additional Requirements "CI Gate"]
- 트리거: `pull_request` + `push` to main
- 잡 7종(AC7 매핑):
  1. `lint-format-api` — `ruff check api/` + `ruff format --check api/`
  2. `typecheck-api` — `mypy api/app`
  3. `test-api` — postgres + redis service container, `uv run pytest --cov`, coverage ≥ 70% 미달 시 fail
  4. `typecheck-web` — `pnpm --filter web tsc --noEmit` (단 워크스페이스 X이므로 `cd web && pnpm tsc --noEmit`)
  5. `typecheck-mobile` — `cd mobile && pnpm tsc --noEmit`
  6. `openapi-diff` — `test-api` 잡에서 OpenAPI schema dump → 기존 `web/src/lib/api-client.ts`와 git diff 비교, 차이 발생 시 fail
  7. `alembic-dry-run` — `cd api && uv run alembic upgrade head --sql` 결과를 PR 코멘트로 게시 (실패 시 fail, 출력은 정보 게시)

- 모든 잡 fail 시 머지 차단 (`branch protection rule`은 README에 SOP 명시 — 본 스토리는 *워크플로우 작성*까지, GitHub UI 설정은 SOP에 위임).
- `pre-commit` hook은 본 스토리 **OUT** (Epic 8에서 도입 가능) — CI에서 lint 게이트로 충분.

### 보안·시크릿

[Source: architecture.md#Authentication & Security, epics.md#Security & Auth]
- **모든 시크릿은 Railway secrets / Vercel env / GitHub Actions secrets에서만 관리**. `.env.local`은 dev 전용으로 gitignored.
- `.env.example`은 변수명 + 설명 + (선택) placeholder만. 실제 키 절대 커밋 금지.
- JWT secret 분리: `JWT_USER_SECRET`(HS256) ↔ `JWT_ADMIN_SECRET`(HS256, 별 issuer claim) — 본 스토리에서는 env 정의 + `app/core/config.py` 로딩까지. 실제 토큰 발행은 Story 1.2.
- Sentry `before_send` hook은 자리표시자(`mask_event`) — NFR-S5 정규식 마스킹은 Story 1.2/Epic 8에서 정교화.
- structlog `mask_processor`도 동일하게 자리표시자.

### Project Structure Notes

- **architecture.md의 폴더 트리(line 569-841)는 8주 종료 시점 *목표* 구조** — 본 스토리에서는 위 "Story 1.1 종료 시점 골격"에 명시된 항목만 생성. premature scaffolding 안티패턴 회피.
- `_bmad-output/`은 `.gitignore` 제외 (BMad 산출물 트래킹 유지).
- `docs/sop/`, `docs/faq/`, `docs/runbook/` 폴더는 *빈 디렉토리 placeholder*(`.gitkeep`)만 본 스토리에서 생성 — 내용은 Epic 8이 채움.
- pnpm workspace `package.json` 루트 작성 **금지** — 단일 repo + multi-folder 결정 위반.

### Detected Conflicts / Variances

| # | 이슈 | 해결 |
|---|------|-----|
| 1 | architecture.md line 216에 `pgvector/pgvector:pg16`, line 285·epics line 418에 `pg17` | **pg17 사용** (Story AC + 데이터 결정 표가 일치하는 쪽). 본 스토리 종료 시 architecture.md line 216을 pg17로 보정 PR 코멘트 첨부 (1줄 수정) |
| 2 | Tailwind 버전 — architecture에 v3, Next.js 16 starter는 v4 디폴트일 가능성 | starter 명령(`--tailwind`)이 결정 — 결과물 버전을 README에 명시. 향후 PR에서 한쪽 통일 |
| 3 | ESLint 설정 — `.eslintrc.json` (epics.md line 426) vs Next 16의 flat config(`eslint.config.js`) 가능성 | starter 결과물을 따른다. 두 형식 중 starter가 생성하는 것을 그대로 커밋 |
| 4 | `next-auth` 사용 여부 — architecture.md는 *회피* 명시, Next 16 인증 패턴 변화 가능 | architecture 결정 우선 — `next-auth` 의존성 추가 금지. Story 1.2에서 백엔드 JWT를 httpOnly 쿠키로 직접 교환 |

### References

- 부트스트랩 명령 시퀀스: [Source: architecture.md#Implementation Handoff "First Implementation Priority" (line 1141-1169)]
- Story AC 원문: [Source: epics.md#Story 1.1 (line 410-426)]
- 폴더 구조 목표: [Source: architecture.md#Complete Project Directory Structure (line 569-841)]
- 데이터 결정 (Postgres 17 + pgvector 0.8.2): [Source: architecture.md#Data Architecture (line 285)]
- 명명 규칙: [Source: architecture.md#Naming Patterns (line 386-431)]
- API 응답 표준: [Source: architecture.md#API & Communication Patterns (line 314-326), #Format Patterns (line 455-471)]
- CI 게이트: [Source: architecture.md#Infrastructure & Deployment (line 346), epics.md#Additional Requirements "CI Gate" (line 268-272)]
- 환경변수: [Source: epics.md#Additional Requirements "Security & Auth" (line 249-258), architecture.md#Authentication & Security (line 309-310)]
- LangSmith 통합: [Source: architecture.md#"LangSmith 외부 옵저버빌리티 (Architecture 추가 결정 3)" (line 232-242)]
- 외주 인수인계 KPI(8시간): [Source: epics.md NFR-M7 (line 177)]

## Dev Agent Record

### Agent Model Used

(채울 것: dev 에이전트 모델/버전)

### Debug Log References

### Completion Notes List

### File List

(채울 것: 본 스토리에서 생성·수정한 파일 전체 경로 목록)
