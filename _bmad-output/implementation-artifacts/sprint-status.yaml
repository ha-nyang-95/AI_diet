# generated: 2026-04-27
# last_updated: 2026-05-07 (Story 4.4 review → done — CR 4 patch 적용: ① PATCH null-removal 단일 ratio 키 → DB partial-set 진입 차단(merged invariant 재검증, rollback + 400) ② NFR-S5 `users.macro_goal.updated` raw user_id → `_mask_user_id` 마스킹 정합 ③ AC7 누락 — `_CITATION_*` 5 상수 import-time `(출처: ...)` regex fail-fast 가드 추가 ④ 광고 가드 `apply_replacements` 단일 패스 → 수렴 루프(3회 cap) — spec line 110 *"치환 후 재검사"* 정합. 회귀 가드 1건 신설(`test_patch_macro_goal_null_removes_single_ratio_rejected_400` — partial-set 진입 차단 + rollback 보존). Defer 3건 → DF129-131(JSONB merge race / mobile-web api-client SOT 중복 / fetch_user_profile TypeError 무방어). 1002 passed/11 skipped/0 failed + coverage 85.31% + ruff/format/mypy 0 에러 + tsc/lint 회귀 0건. epic-4 in-progress → done(Story 4.4 클로저).)
# last_updated: 2026-05-04 (Story 3.8 ready-for-dev — `app/core/observability.py` 신규(LangSmith Client + mask_run_inputs/outputs hook + init_langsmith singleton, `app.core.sentry._MASKED_KEYS` import 합집합 SOT — NFR-S5 1지점 + LangGraph state PII 키 8건 추가: weight_kg/height_cm/allergies/parsed_items/food_items/clarification_options/feedback_text/feedback) + `scripts/upload_eval_dataset.py` 신규(한식 100건 → LangSmith Dataset `balancenote-korean-foods-v1` argparse 5옵션 + idempotent skip + --force append + --dry-run + LANGSMITH_API_KEY 가드) + `langsmith>=0.7.31` SDK bump(GHSA-rr7j-v2q5-chgv `events` 배열 PII leak 패치) + `.env.example` LANGCHAIN_TRACING_V2=false 디폴트(로컬 dev 송신 0 + Free tier 5K 보호) + `PRIVACY_POLICY_KO/EN` 제3자 제공·국외 이전 단락 + version bump + `docs/observability/langsmith-dashboard.md` 신규(보드 read-only 링크 + 영업 데모 4시나리오 + 마스킹 검증 가이드) + `docs/runbook/secret-rotation.md` LangSmith 키 회전 5단계 추가 + `.github/workflows/ci.yml` `langsmith-eval-regression` 옵션 job(workflow_dispatch + `langsmith-eval` 라벨 트리거, continue-on-error true, RUN_EVAL_TESTS=1 한식 100건 정확도 ≥90% PR 코멘트 권장 경고). LangGraph native tracing 활성은 LANGCHAIN_TRACING_V2 env 1개(`compile_pipeline`/노드/서비스/라우터 코드 변경 0 강제 — D4). 단위 테스트 ≥25(observability ≥12 + tracing 활성 ≥3 + upload script 5 + dependency_versions 2 + privacy 2 + lifespan smoke). Story 3.3-3.7 회귀 0건 가드. Branch story-3.8. Status: backlog → ready-for-dev.)
# last_updated: 2026-05-04 (Story 3.7 ready-for-dev — `POST /v1/analysis/stream` SSE + `GET /v1/analysis/{meal_id}` polling fallback + `POST /v1/analysis/clarify` resume 3 endpoint(`api/app/api/v1/analysis.py` 신규) + `app/services/sse_emitter.py` 신규(chunk_text + 6 event builders + AnalysisDonePayload Pydantic) + `AnalysisStreamError` 계층 4 서브클래스(MealNotFound/CheckpointerUnavailable/ClarifyValidation + base) + 5번째 이벤트 type `event: clarify` Story 3.4 needs_clarification 분기 노출(spec extension 1건) + `event: progress` heartbeat TTFT 보장(NFR-P3) + pseudo-streaming 24자/30ms 본문 chunking(D1 — true LLM-token은 Story 8.4 polish 보류) + `RATE_LIMIT_LANGGRAPH = "10/minute"` per-IP 적용 + dual PIPA gate(basic + automated_decision) stream/clarify 강제, polling은 basic만(Art.35 정합) + `mobile/features/meals/useMealAnalysisStream.ts` orchestrator hook(`react-native-sse` SSE + 1.5초 polling 강등 + 30회 timeout) + `mobile/features/meals/ChatStreaming.tsx`(token append + clarify chips + done card + DisclaimerFooter + Retry) + `mobile/features/meals/MacroCard.tsx`(NFR-UX3 접힘 보조 카드) + `[meal_id].tsx` "AI 분석 시작" CTA 활성화 + `react-native-sse ^1.2` 모바일 의존성. `meal_analyses` 테이블 신규(0012 마이그레이션) + UPSERT helper(`event: done` 시점 INSERT ... ON CONFLICT DO UPDATE, graceful 실패 swallow) + `feedback_summary` derive helper(첫 평가 문장 → 120자) + `GET /v1/meals` JOIN(`selectinload(Meal.analysis)` + `lazy="raise_on_sql"` N+1 가드) → 재로그인 시 한 주 식습관 한눈에 + 이전 분석 결과 즉시 표시 UX(D5 hwan 피드백 후 IN으로 변경 — 식단 *관리* 앱 본연 정합). NFR-S5 마스킹 패턴 정합(meal_id/mode/phase/token/citation/used_llm/fit_score/latency_ms only). Branch story-3.7. Status: backlog → ready-for-dev.)
# last_updated: 2026-05-03 (Story 3.6 ready-for-dev — `generate_feedback` 노드 stub 폐지 + 실 인용형 피드백 흐름 12단계(가이드라인 검색 → cache lookup → dual-LLM router → 인용 검증·재생성 → 광고 가드·재생성 → 출력 구조 검증·재생성 → safe fallback) + `app/adapters/llm_router.py` 신규(OpenAI gpt-4o-mini 메인 tenacity 3회 1s/2s/4s backoff → Anthropic claude-haiku-4-5 fallback → 양쪽 실패 시 Sentry capture_message + safe FeedbackOutput) + `app/adapters/anthropic_adapter.py` 신규(lazy singleton + transient subset retry + prompt-driven JSON parse) + `app/domain/ad_expression_guard.py` 신규(진단/치료/예방/완화/처방 정규식 + 굴절형 매칭 + 안전 워딩 치환 + 인용 본문 보존) + `app/graph/prompts/feedback.py` 신규(KOREAN_MEAL_CONTEXT 25/35/30/10 + COACHING_TONE_GUIDE Noom-style + CITATION_FORMAT_RULE + OUTPUT_STRUCTURE_RULE) + `app/graph/nodes/_feedback_postprocess.py` 신규(인용 검증 / 구조 검증 / 안전 fallback) + `app/rag/guidelines/search.py` 신규(KnowledgeChunk HNSW + applicable_health_goals 필터 top-K) + `FeedbackLLMOutput` + `KnowledgeChunkContext` IO 모델 + `LLMRouterError` 4 클래스 + Redis cache `cache:llm:{sha256(NFC raw_text + profile_hash)}` 24h TTL + `anthropic>=0.40` 직접 의존성 승격 + 신규 `llm_main_model`/`llm_fallback_model`/`llm_cache_ttl_seconds` Settings. T4 KPI 100건 eval marker + T10 KPI ≥10 케이스 ad-guard. Story 3.3/3.4/3.5 회귀 0건 가드(compile_pipeline / state.py 기존 모델 변경 X). Branch feat/story-3.6. Status: backlog → ready-for-dev.)
# last_updated: 2026-05-02 (Story 3.4 ready-for-dev — `retrieve_nutrition` 3단(alias→exact→HNSW) + `parse_meal` DB 우선/LLM fallback + `rewrite_query` 실 LLM + `request_clarification` 신규 + Self-RAG clarify 3-way 분기 + `analysis_service.aresume` Command 패턴 + ClarificationOption + korean_foods_100.json + eval marker. Story 3.3 회귀 0건. Branch feat/story-3.4. Status: backlog → ready-for-dev.)
# last_updated: 2026-05-01 (Story 3.3 ready-for-dev — LangGraph 6노드 + Self-RAG 분기(rewrite_attempts < 1 1회 한도, 무한 루프 차단) + AsyncPostgresSaver(lg_checkpoints schema 격리, Alembic 외 lib .setup()) + per-node tenacity retry(stop_after_attempt 2 + wait_exponential 1-4) + Pydantic IO 모델 6세트(MealAnalysisState TypedDict + ParseMealOutput/RetrievalResult/EvaluationDecision/UserProfileSnapshot/FitEvaluation/FeedbackOutput) + Sentry transaction(op=analysis.pipeline) + 노드별 child span(op=langgraph.node) + analysis_service 진입점(run/aget_state/aresume) + AnalysisGraphError 4 서브클래스(NodeError/CheckpointerError/StateValidationError/RewriteLimitExceededError) + psycopg[binary,pool]>=3.2 직접 의존성 승격(transitive→직접) + langgraph-checkpoint-postgres>=3.0 강화 + lifespan 4 자원(checkpointer/pool/graph/analysis_service) graceful 분기 + perf 골격(@pytest.mark.perf NFR-P4 ≤3s + NFR-P5 +2s + T1 100회 ≤2%) skip 디폴트. 6노드 중 evaluate_retrieval_quality만 *실 결정 로직*(Self-RAG 핵심) + fetch_user_profile은 *실 DB 조회* — 나머지 5 노드는 결정성 stub(Story 3.4-3.6 책임 영역). PIPA 게이트/Rate limit/SSE/LangSmith/실 LLM은 모두 OUT — 후속 스토리. branch feat/story-3.3 분기 완료. Status: backlog → ready-for-dev.)
# project: AI_diet
# project_key: NOKEY
# tracking_system: file-system
# story_location: {project-root}/_bmad-output/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)

# last_updated: 2026-05-05 (Story 3.9 ready-for-dev — 에픽 3 후 polish 일괄 처리. 23건 NOW-ACTIONABLE을 16 AC + 10 Task로 그룹화: A. 인프라 토폴로지 Railway+Cloudflare proxy IP 5건(DF111+W2 1.2+W10 1.3+W1 1.4+W17 1.5) — `api/app/core/proxy.py` 신규 + `_CLOUDFLARE_IPV4`/`_IPV6` frozenset SOT + `get_real_client_ip` CF-Connecting-IP 우선 헬퍼 + 5 callsite 일괄 swap + `docs/runbook/deployment.md` + `cloudflare-ip-refresh.md` 신규. B. 약관 major/minor 분기(W4 1.3) — `LEGAL_DOCUMENTS`에 `version_type: Literal["major","minor"]` + minor mismatch는 `X-Consent-Update-Available` 헤더 안내. C. Idempotency-Key 30일 TTL(DF50) — `scripts/cleanup_idempotency_keys.py` + GHA daily cron. D. Vision $5/일 cap + 24h 캐시 + image_key(DF4/5/6 2.3) — `cache:llm:vision:{sha256(image_key)}` Redis + `cost:vision:daily:{YYYY-MM-DD}` 카운터 + `gpt-4o-mini` 다운그레이드 fallback. E. 식약처 데이터 보강(DF102/103) — `max_pages` 100→500 + `DB_GRP_CM=D|P` 양 그룹 fetch + 외식 영업 50종 수동 CSV(짜장면/짬뽕/탕수육/마라탕/떡볶이/순대/김밥 변형 등) + bootstrap_seed 재실행 + LangSmith dataset 재 upload(T3 KPI ≥90% 회귀 가드). F. Epic 3 잔여 cleanup 11건 — DF89/99 aresume Command(resume) + sanitize layer + DF91 deterministic 노드 retry opt-out + DF115 SSE T5 KPI perf marker + DF100 `_USED_LLM_OPENAI` Literal 확장 + DF104 citation fullwidth/whitespace 변형 수용 + DF9 Vision 환각률 측정 marker + DF101 mock LLM fixture conftest 통합 + DF28 MealMacros upper bound + DF38 폰트 200% allergen header 회귀 + DF21 buster SOP 문서화 + DF18/26 단건 `GET /v1/meals/{meal_id}` endpoint + 모바일 `useMealQuery`. SOT: `deferred-work.md#결정 사항 (2026-05-05)` + 인라인 `**결정 (2026-05-05)**` 노트. 신규 의존성 0건. 신규 단위 테스트 ~60건(현 776 → ~836+) + coverage ≥84% 유지. 회귀 0건 가드 — Story 1.x/2.x/3.x 기존 동작 보존 의무. Branch story-3-9. Status: backlog → ready-for-dev.)
generated: 2026-04-27
# last_updated: 2026-05-06  # Story 4.1 done — CR 3-layer adversarial(Blind Hunter / Edge Case Hunter / Acceptance Auditor) ~50 raw → 5 patch + 16 defer + 16 dismiss. **P1**(spec violation): RFC 7807 code 카탈로그 미발화 — Pydantic field_validator의 ValueError가 글로벌 핸들러에서 `code=validation.error`로 평탄화. Pydantic 가드를 type-only로 제한 + 라우터에서 regex 검증 후 `NotificationTimeInvalidFormatError` / `NotificationTokenInvalidError` raise → spec catalog `code=notification.time.invalid_format` / `code=notification.token.invalid` 발화. test 2건 expected code 갱신. **P2**(functional): `platform: 'ios'` 하드코드를 `resolveDevicePlatform()` 헬퍼(`mobile/lib/push.ts` SOT)로 전환 — Android/Web 사용자도 정확한 platform 등록. **P3**(spec violation): `mobile/.env.example`에 `EXPO_PUBLIC_EAS_PROJECT_ID=` 1줄 추가(AC14 정합). **P4**(DRY): `PUSH_PROMPTED_STORAGE_KEY` 2 파일 중복 선언을 `mobile/lib/push.ts` SOT 1지점 + `(tabs)/_layout.tsx` + `auth.tsx` import로 통합. **P5**(editorial): `register_push_token` docstring "단일 `async with db.begin()`" → AsyncSession implicit auto-begin + 단일 commit 정확 표현(race window + Story 8.4 hardening note 추가). **Defer 16건** deferred-work.md 등록 — 특히 W1(AC15 mobile ~12 jest 테스트, Story 8.4 sprint plan에 jest 인프라 scope 부재 검증, Story 8.4 amendment 또는 신규 dedicated story 결정 필요) + W2(register_push_token TOCTOU SELECT FOR UPDATE hardening). **Dismiss 16건** 각 항목별 사유 명시(spec-compliant defer / 프로젝트 컨벤션 / over-engineering). 회귀 0건: pytest 843 passed/11 skipped/0 failed + ruff/format/mypy 0 + mobile tsc + lint 0 + web tsc 0. Branch story-4-1. Status: review → done.

last_updated: 2026-05-06  # Story 4.3 review — DS 완료(Amelia). Epic 4 세 번째 스토리(epic-4 in-progress 유지). Web 주간 리포트 4종 차트 + NFR-P8 1.5초 + NFR-A5 a11y baseline. **신규 백엔드 4 모듈**: (1) `app/api/v1/reports.py`(`GET /v1/reports/weekly` endpoint + `Depends(require_basic_consents)` + RFC 7807 400 매핑 `reports.invalid_date_range` + 30일 cap). (2) `app/services/report_service.py`(`get_weekly_report` aggregation + Pydantic 4 응답 모델 SOT — 순환 import 회피 결정 / Story 4.4 forward-compat `insights: None = None` 슬롯 + Sentry transaction `op="reports.weekly"` + child span `op="reports.aggregate"` + structlog masked `user_id u_{8char}`). (3) `app/core/exceptions.py:ReportsError` + `WeeklyReportInvalidDateRangeError` 신규. (4) `app/main.py` 라우터 wire 1줄. **도메인 SOT 재배치**: Story 3.5 `_LABEL_SUBSTRING_EXCLUSIONS` / `_ALIAS_TEXT_EXCLUSIONS` / `_SKIP_SUBSTRING_LABELS` / `ALLERGEN_ALIAS_MAP` 4 constant를 `fit_score.py` → `allergens.py` 재배치 + `contains_allergen` 헬퍼 신규(중복 정의 0). `fit_score.py`는 import로 인계. **`PROTEIN_TARGET_BY_GOAL` SOT**: `app/domain/health_profile.py` `MappingProxyType` 4 health_goal × (lower, upper) g/kg/day 권장 범위(KDRIs 2020 + 대한비만학회 2024 + ISSN 2017 + 대한당뇨학회 2023 4 출처) + `get_protein_target` 헬퍼. **신규 web 8 모듈**: `lib/reports.ts`(`getServerSideWeeklyReport` SSR fetch + `computeWeeklyWindowKst` KST 7일 윈도우) + `features/reports/{api,WeeklyReport,MacroChart,CalorieChart,ProteinChart,AllergyExposureChart}.tsx` 6 + `app/(user)/dashboard/weekly/page.tsx` Server Component(4 가드 + SSR fetch + WeeklyReport client component 렌더). 각 차트 `<details>` 데이터 테이블 fallback + `aria-label` + `tabIndex={0}`(NFR-A5). `useSyncExternalStore`(server snapshot=false / client snapshot=true) SSR-Hydration mismatch 회피 표준 React 패턴. **신규 의존성 1건**: `recharts ^3.8.1`(2026-04 latest stable, React 19 호환). **신규 docs**: `docs/runbook/web-perf-check.md` Lighthouse Mobile SOP. **Spec deviation 1건**: `WeeklyReportResponse.insights: None = None` Pydantic baseline 항상 None — Story 4.4가 type narrow 의무. pytest 867 → **934 passed / 11 skipped / 0 failed** (+67), coverage 84.85% → **85.40%** (+0.55pp). ruff/format/mypy 0 errors + web/mobile tsc/lint 0 errors. Story 1.x/2.x/3.x/4.1/4.2 회귀 0건. Branch story-4-3. Status: in-progress → review.

last_updated: 2026-05-07  # Story 4.4 review — DS 완료(Amelia). Epic 4 마지막 스토리. 인사이트 카드 + 매크로 목표 조정 + 자동 반영. **신규 백엔드 7 모듈**: (1) `app/domain/macro_goal.py`(`MacroGoal` Pydantic 5 필드 partial + `@model_validator` ratio all-or-none + sum=100 + `MacroGoalPatchRequest` `model_fields_set` at-least-one 가드 + `MACRO_GOAL_CHECK_SQL` SOT 모듈 상수). (2) `app/domain/insights.py`(`InsightKind` Literal 4종 + `InsightCta`/`InsightCard` Pydantic frozen + `generate_weekly_insights` 결정성 룰 4 카드 종류 + 5 인용 SOT 모듈 상수 + import-time ad guard `_TEMPLATE_SAMPLES_FOR_AD_GUARD` SOT drift 차단 + `_KIND_PRIORITY`/`_SEVERITY_PRIORITY` cap 4 + sub-cap 2). (3) `alembic/versions/0015_users_macro_goal.py`(JSONB 컬럼 + `ck_users_macro_goal_shape` CHECK 제약 SOT는 `app/domain/macro_goal.py` import — Pydantic 1차 + DB 2차 double gate). (4) `api/v1/users.py`에 `PATCH /me/macro_goal` + `GET /me/macro_goal` 2 endpoint 추가(JSONB merge SQL `COALESCE(macro_goal, '{}'::jsonb) || :patch` + null 송신 = key 삭제 분기 + `model_dump(exclude_unset=True)` 명시 송신 SOT + `MacroGoalInvalidError` RFC 7807 `code=user.macro_goal.invalid`). (5) `core/exceptions.py:UserError` + `MacroGoalInvalidError` 신규. (6) `services/report_service.py` `WeeklyReportResponse.insights` type narrow `None → list[InsightCard] | None` + `generate_weekly_insights` 호출 + `_apply_ad_guard_to_insights` defensive 2차 가드(structlog `insight.ad_guard.replaced` NFR-S5 본문 X) + Sentry child span `op="reports.insights"` + 4 boolean attribute. (7) `workers/nudge_scheduler.py` `_build_nudge_body(macro_goal)` 헬퍼 + `_fetch_eligible_users` SQL select 4-tuple `u.macro_goal` 추가 + sweep loop body 자동 반영(macro_goal=None default `NUDGE_BODY` fallback). **LLM 분석 자동 반영(AC8)**: `graph/state.py:UserProfileSnapshot.macro_goal: MacroGoal | None = None` 필드 + `graph/nodes/fetch_user_profile.py` JSONB→MacroGoal 변환(ValidationError graceful None) + `graph/nodes/generate_feedback.py:_build_profile_hash` body 확장(`macro_goal_serialized = json.dumps(model_dump(exclude_none=True), sort_keys=True) | "null"`) → cache miss 자동 트리거 + `graph/prompts/feedback.py:format_macro_goal_summary` 헬퍼 + `build_user_prompt` conditional `macro_goal_block`. **신규 web 8 모듈**: `lib/macro-goal.ts`(`getServerSideMacroGoal` SSR fetch 패턴 정합) + `features/settings/{api,MacroGoalForm}.tsx` 2(react-hook-form + zod schema 첫 도입 — 5 number input + ratio refine 검증 + Toast UX + `useMacroGoalMutation` queryKey invalidate `["macro-goal"]`+`["weekly-report"]`) + `features/reports/{InsightCard,InsightCardList}.tsx` 2 + `app/(user)/settings/macro-goal/page.tsx` Server Component(4가드 정합). **WeeklyReport.tsx 갱신**: placeholder section → `<InsightCardList insights={report.insights} />` mount + CTA placeholder `<button disabled>` → `<Link href="/settings/macro-goal">` 활성. **AGENTS.md** react-hook-form/zod 도입 완료 표기 + 폼 패턴 강제 룰. **신규 의존성 0건**(react-hook-form/zod/@hookform/resolvers 모두 web/package.json 기존 설치). **단위 테스트 신규 ~37건**: domain/insights 16(4 카드 종류 happy/threshold + cap + frozen + import-time guard) + domain/macro_goal 12(Pydantic 검증) + api/v1/users macro_goal endpoint 8(인증/consent/GET/PATCH happy/partial/sum invalid/null=key 삭제/empty body) + db 0015 마이그레이션 3(default null + CHECK 거부 + 정상 shape) + workers/nudge_scheduler body 자동반영 4(default/empty/protein challenge/sweep integration) + report_service insights 5(빈 list + protein deficit + total cap + KDRIs 우선순위 + ad guard pass) + graph 7(fetch_user_profile JSONB→MacroGoal 2 + generate_feedback profile_hash 변경/안정 2 + prompts macro_goal_summary 3 + build_user_prompt conditional 2). **회귀**: pytest 944 → **1001 passed / 11 skipped / 0 failed** (+57). coverage 85.33% → **85.56%** (+0.23pp). ruff/format/mypy 0 errors + web tsc/lint 0 errors + mobile tsc/lint 0 errors. Story 1.x/2.x/3.x/4.1/4.2/4.3 회귀 0건. **Spec deviation 1건**: AC2 `MacroGoalPatchRequest` at-least-one 가드는 *명시 송신* 기준(``model_fields_set``)으로 변경 — null 단일 송신은 *해당 키 삭제* valid PATCH로 처리(REST PATCH 표준 정합 + epics.md 의도). 5 필드 모두 None default + 빈 body `{}`만 거부. test_users_macro_goal.py 정합. **OpenAPI client 자동 갱신**: IN_PROCESS=1 gen:api 실행 — `web/mobile/lib/api-client.ts` 신규 endpoint 2 + `MacroGoal`/`MacroGoalPatchRequest`/`InsightCard`/`InsightCta` types 추가 + `WeeklyReportResponse.insights` type narrow 반영. Branch story-4-4. Status: in-progress → review.

# 원본 ready-for-dev 메모(보존):
# last_updated: 2026-05-07 — Story 4.4 ready-for-dev. 신규 의존성 0건. 신규 컴포넌트 6종(백엔드). (2) `app/domain/insights.py`(`InsightCard` Pydantic SOT + `InsightKind` Literal 4종 protein_deficit/calorie_diff/macro_ratio_drift/allergen_exposure + `generate_weekly_insights` 결정성 룰 + 5 인용 SOT 모듈 상수 KDRIs/대비학회/식약처/본인 설정 + import-time ad_guard 검증). (3) `alembic/versions/0015_users_macro_goal.py`(JSONB 컬럼 + `ck_users_macro_goal_shape` CHECK 제약 — Pydantic 1차 + DB 2차 double gate). (4) `api/v1/users.py:PATCH /me/macro_goal` + `GET /me/macro_goal` 2 endpoint(JSONB merge SQL `COALESCE(macro_goal, '{}'::jsonb) || :patch` + null 송신 = key 삭제 분기 + `MacroGoalInvalidError` RFC 7807 `code=user.macro_goal.invalid`). (5) `services/report_service.py` `WeeklyReportResponse.insights` type narrow `None → list[InsightCard] | None` + `generate_weekly_insights` 호출 + ad guard pass + Sentry child span `op="reports.insights"`. (6) `workers/nudge_scheduler.py` `_build_nudge_body(macro_goal)` 헬퍼 + sweep loop body 자동 반영(macro_goal=None default `NUDGE_BODY` fallback). **LLM 분석 자동 반영(AC8)**: `graph/state.py:UserProfileSnapshot.macro_goal` 필드 + `graph/nodes/fetch_user_profile.py` JSONB→MacroGoal 변환 + `graph/nodes/generate_feedback.py:_build_profile_hash` body 확장(macro_goal 변경 시 sha256 변경 → 다음 분석 cache miss → 새 LLM 호출 자동) + `graph/prompts/feedback.py:KOREAN_MEAL_CONTEXT` conditional 블록 + `format_macro_goal_summary` 헬퍼. **신규 web 6 모듈**: `lib/macro-goal.ts`(`getServerSideMacroGoal` SSR fetch) + `features/settings/{api,MacroGoalForm}.tsx` 2(react-hook-form + zod schema 첫 도입 — 5 number input + ratio refine 검증 + Toast UX) + `features/reports/{InsightCard,InsightCardList}.tsx` 2 + `app/(user)/settings/macro-goal/page.tsx` Server Component(4가드 정합). **WeeklyReport.tsx 갱신**: placeholder section → `<InsightCardList insights={report.insights} />` mount + CTA placeholder `<button disabled>` → `<Link href="/settings/macro-goal">다음 주 권장 매크로 조정</Link>` 활성. **Forward-compat 유지**: Redis profile cache(`cache:user:{user_id}:profile` architecture.md:296 미구현)는 코멘트만 추가(Story 8.4 polish forward) — LLM cache invalidate는 _build_profile_hash 자동 메커니즘. TDEE 비교 calorie_diff 우선순위 ②는 Story 5.1 `users.sex` 컬럼 추가 후 활성화. **모바일 영향 0건**(SSE/feedback_text 흐름이 자동 반영). **FR24 인용 SOT** 5종 모듈 상수 + **FR25 광고 가드 pass** post-process(템플릿 사전 검증 + import-time 가드). **NFR-S5 마스킹 정합**(body 본문 logging X, user_id `u_{8char}`). 단위 테스트 신규 ~37건(insights 14 + macro_goal Pydantic 5 + users macro_goal endpoint 8 + 0015 마이그레이션 2 + nudge body 3 + report_service insights 5). pytest 944 → ~981 passed/11 skipped/0 failed + coverage ≥84% 유지. 회귀 0건 가드 — Story 1.x/2.x/3.x/4.1/4.2/4.3 기존 모든 테스트 통과 의무. OpenAPI schema diff 신규 endpoint 2건 + 4 types 신규 + 1 type narrow. Branch story-4-4(master에서 분기 완료). Status: backlog → ready-for-dev.

# last_updated: 2026-05-07  # Story 4.3 done — CR 3-layer adversarial(Blind 33 / Edge 40 / Auditor 12 = 85 raw) → 3 decision-needed + 22 patch + 13 defer + 47 dismiss. **DN-1**(BLOCKER, BMR `sex="female"` 하드코드 → tdee=None graceful, Story 5.1까지 모든 사용자 빈 TDEE 라인): 잘못된 default로 ~166 kcal 어긋난 TDEE 송신 회귀 차단(spec AC1/AC7 정합 회복). **DN-2**(MAJOR, feedback_text negation false-positive): `_NEGATION_PATTERNS = ("않","없","아니","아닙","제외")` + `_NEGATION_WINDOW_CHARS=12` 매칭 직후 윈도우 가드 — "우유는 들어있지 않아 안심" → False(AllergyExposureChart 과대보고 차단). **DN-3**(MAJOR, 차트 a11y): spec 정합 `tabIndex={0}` 유지 + `aria-describedby={tableId}` 4 차트 추가 — chart→table ARIA 연결 SR 사용자 즉시 인식. **P1**(BLOCKER, NFR-P8 무력화): `app/services/report_service.py` SQL `WHERE ate_at >= from_dt_utc AND ate_at < to_dt_exclusive_utc` UTC bracket 추가 — 사용자 전체 history 로드 회귀 차단(이전: 1년 사용자 ~1000 row + selectinload 메모리 폭증). **P2**(spec deviation): allergen 매칭 입력에서 `raw_text` 제거(spec AC1 정합 — `parsed_items[].name` + `feedback_text` SOT). **P3**(robustness): `meal.ate_at` naive datetime 가드 — `replace(tzinfo=UTC)` fallback. **P4**: `WeeklyMacros Field(ge=0)` `max(0.0, round(...))` clamp(round(-0.0001) = -0.0 ValidationError 회귀 차단). **P5**: `int(a.energy_kcal or 0)` None graceful + `round(float(...))`(int truncate 회귀 차단). **P6**: `compute_tdee` `(ValueError, KeyError)` catch(legacy activity_level KeyError 500 회귀 차단). **P7**: `AllergenLiteral` `assert` → `if/raise RuntimeError`(SOT drift fail-fast 보존, `python -O` strip 차단). **P8**(`web/src/lib/reports.ts`): KST window `Intl.DateTimeFormat("en-CA", {timeZone:"Asia/Seoul"})` SOT 전환(manual 9h offset 회피, runtime TZ fragile 차단). **P9**: `getServerSideWeeklyReport` 401/5xx redirect/throw 제거 → `if (!response.ok) return null` 일관(`getServerSideUser` 패턴 정합). **P10**(`weekly/page.tsx`): `fetchUserAllergies` 401 시 `/api/auth/cleanup` redirect + 5xx 시 null 반환(silent `[]` 회귀 차단). **P11**(`WeeklyReport.tsx`): 빈 주에도 NFR-A5 데이터 테이블 fallback 노출(SR 사용자 zero-count 표 인식). **P12**(`ProteinChart.tsx`): `connectNulls` 제거(비기록일 직선 보간 misleading 차단). **P13**(`CalorieChart.tsx`): `tdee !== null && tdee !== undefined && tdee > 0` 가드(ReferenceLine y=0 misleading 차단). **P14**(`AllergyExposureChart.tsx`): `noUserAllergies`/`fetchFailed` 시 `<details>` 자체 미렌더 + `monitored_allergies` 카운트 캡션. **P15**: `try/except Exception: transaction.set_status("internal_error"); raise` Sentry 패턴(Story 4.2 retro 정합 — 실패율 visibility 0 회귀 차단). **P16**: 라우터 dual guard 제거 + service `WeeklyReportInvalidDateRangeError` 일원화(typed exception SOT). **P17 → DISMISS**(적용 시점 재분류): peer endpoint 모두 explicit `@limiter.limit` 미적용 + project comment SOT — Story 8.4 polish에서 일괄 적용. **P18**: `PROTEIN_TARGET_BY_GOAL` import-time SOT drift 가드(`if set(...) != set(HEALTH_GOAL_VALUES): raise`) + `.get()` graceful. **P19**: structlog `report.weekly.complete` 이벤트 `from_date`/`to_date` 추가(slow 요청 디버깅 stitching). **P20**: vacuous test fix — `session.expunge(loaded)` + ORM 메모리 인젝션으로 22-out-of-bounds 필터 path 실 검증. **P21**(`features/reports/api.ts`): `WeeklyReportFetchError` typed + `staleTime: 60_000` + `retry: (count, err) => !(4xx) && count < 1`(4xx hammering 회귀 차단). **P22**(`CalorieChart.tsx`): 평균 분모 = 비기록일 제외(`chartData.filter(d => d.kcal > 0).length`) + 라벨 *"기록일 평균"*(misleading 차단). **Defer 13건** DF116-DF128 deferred-work.md 등재(allergen short labels 가드 / weight_kg sanity / DoS cap / NFC 캐싱 / 기타 alias / monitored_allergies / 빈 주 dashboard 링크 / 단일일 지원 / parsed_items shape 경고 / protein 0 표시 / Tooltip formatter / 연도 경계 / Lighthouse 실측). pytest 934 → **944 passed / 11 skipped / 0 failed** (+10 신규 가드 — allergen negation 7 + report_service SQL 윈도우 1 + 30일 cap 1 + negation false positive 1), coverage 85.40% → **85.33%** (-0.07pp 정상 — 신규 SQL 분기·exception path 추가). ruff/format/mypy 0 errors + web tsc/lint 0 errors + mobile tsc 0 errors. Story 1.x/2.x/3.x/4.1/4.2 회귀 0건. Branch story-4-3. Status: review → done.

# last_updated: 2026-05-06  # Story 4.3 ready-for-dev — Epic 4 세 번째 스토리(epic-4 in-progress 유지). Web 주간 리포트 4종 차트 + NFR-P8(7일 ≤ 1.5초) + NFR-A5(키보드 Tab + 데이터 테이블 fallback). **신규 의존성 1건**: `recharts ^3.8.0` (Web only, React 19 공식 지원 since 2.15+). **신규 컴포넌트 8종**: (1) `app/api/v1/reports.py` 신규(`GET /v1/reports/weekly?from_date=...&to_date=...` endpoint + `Depends(current_user)` + `Depends(require_basic_consents)` + RFC 7807 400 mapping + Rate limit 60/min). (2) `app/services/report_service.py` 신규(`get_weekly_report` aggregation — meals LEFT JOIN meal_analyses 단일 SQL + KST 일별 group + protein_g_per_kg 계산 + allergen 노출 매칭 + Sentry transaction `op="reports.weekly"` + child span `op="reports.aggregate"`). (3) `app/domain/allergens.py:contains_allergen` 헬퍼 신규(NFC + Story 3.5 `_LABEL_SUBSTRING_EXCLUSIONS` / `_ALIAS_TEXT_EXCLUSIONS` / `_SKIP_SUBSTRING_LABELS` SOT 재사용 — 메밀↔밀, 도넛↔넛 false-positive 가드 0건 추가). (4) `app/domain/health_profile.py:PROTEIN_TARGET_BY_GOAL` SOT + `get_protein_target` 헬퍼(4 health_goal × (lower, upper) g/kg/day 권장 범위 — KDRIs 2020 + 대한비만학회 2024 1차 출처). (5) `web/src/app/(user)/dashboard/weekly/page.tsx` 신규(Server Component SSR + 4 가드 정합 dashboard/page.tsx 패턴 + 7일 윈도우 KST 계산). (6) `web/src/lib/reports.ts` 신규(`getServerSideWeeklyReport` `import "server-only"` cookie forward 패턴 — Story 1.2 `getServerSideUser` 정합). (7) `web/src/features/reports/{WeeklyReport,MacroChart,CalorieChart,ProteinChart,AllergyExposureChart}.tsx` 5 컴포넌트(recharts BarChart 스택 / ComposedChart + ReferenceLine TDEE / LineChart + ReferenceArea 권장 범위 / horizontal BarChart 알레르기 노출 + 각각 `<details><summary>` 데이터 테이블 fallback NFR-A5 + aria-label). (8) `docs/runbook/web-perf-check.md` 신규(Lighthouse SOP 1페이지 — NFR-P8 측정 명령 + 통과 기준). **WeeklyReportResponse Pydantic 슬롯**: `from_date`/`to_date`/`tdee`/`health_goal`/`protein_target_g_per_kg_lower`/`_upper`/`weight_kg`/`daily_summaries: list[DailySummary]`(7 요소 빈 날 포함) + Story 4.4 forward-compat `insights: None = None` 슬롯(Story 2.4 `analysis_summary` 패턴 정합). DailySummary 필드 5건(kst_date/meal_count/macros/energy_kcal_total/allergen_exposures). **Aggregation SOT**: meals LEFT JOIN meal_analyses 단일 SQL + KST `(ate_at AT TIME ZONE 'Asia/Seoul')::date` group + Story 3.7 `idx_meal_analyses_user_id_created_at` 인덱스 hit + N+1 가드(`selectinload` 또는 explicit JOIN). **TDEE 계산**: `app/domain/bmr.py:compute_bmr_mifflin` + `compute_tdee` SOT 재사용 — BMR 입력 5건(age/weight_kg/height_cm/sex/activity_level) 하나라도 None이면 graceful tdee=None. **Allergen 매칭 의사결정**: substring contains(Story 3.5 SOT 재사용) — `meal_analyses.fit_reason='allergen_violation'` 카운트 또는 jsonb 영속화 옵션 dismiss 사유 명시(Story 3.5 fit_reason은 *심각 노출* 기준이라 false negative). 단위 테스트 신규 ~35건(allergens 12 + health_profile 5 + report_service 10 + reports router 8). pytest 867 → ~902 passed/11 skipped/0 failed + coverage ≥84% 유지. 회귀 0건 가드 — Story 1.x/2.x/3.x/4.1/4.2 기존 모든 테스트 통과 의무. OpenAPI schema diff 신규 1 endpoint + 5 types. Branch story-4-3(master에서 분기 완료). Status: backlog → ready-for-dev.

# last_updated: 2026-05-06  # Story 4.2 done — CR 3-layer adversarial(Blind Hunter 33 / Edge Case Hunter 30 / Acceptance Auditor 5) 68 raw → 5 patch + 7 defer + 56 dismiss. **P1**(mobile UX): cold-start `useLastNotificationResponse` + foreground tap listener double-fire — module-level `_handledIdentifiers` Set + `markPushResponseHandled` export로 dedupe. **P2**(robustness): `ticket.status != "error"` 명시 분기 + `isinstance(ticket.details, dict)` 가드 + `error_code or "unknown"` 라벨 — SDK 미래 버전(`"warning"` 등 unknown status) + non-dict `details` AttributeError 차단 + 비유익 `nudge.ticket_error: None` Sentry 메시지 제거. **P3**(observability): sweep loop unhandled exception 시 `transaction.set_status("internal_error")` — Sentry 대시보드 실패율 0 회귀 차단. **P4**(reliability): `_record_notification` IntegrityError 외 transient DB 에러(OperationalError 등)도 per-user `try/except Exception`으로 swallow → sweep loop 보존(1명 DB 실패가 99명 미발사로 번지지 않음). **P5**(test isolation): `test_lifespan_scheduler_initializes_in_dev_environment` 실 scheduler 백그라운드 thread leak 차단 — `start_scheduler` no-op monkeypatch. **Defer 7건** deferred-work.md 등재(D1 push-then-INSERT race / D2 fetch_receipts SDK 시그니처 / D3 mobile jest infra / D4 users sweep expression index / D5 deep link kind 매핑 / D6 receipts polling / D7 자정 cross today_kst UX semantic). **Dismiss 56건** 사유: (a) 1인 8주 단일 노드 정합 acknowledged(multi-replica는 Story 8.5); (b) 정합 의도(`_NON_PROD_ENVIRONMENTS == _DISABLED_ENVIRONMENTS` 동일 set / `revoke_push_token` SOT 재사용); (c) cosmetic/over-engineering; (d) 멱등 SOT(NOT EXISTS notifications)가 push API double-call 차단. 신규 회귀 가드 테스트 3건(unknown status / details non-dict / DB transient). 회귀 0건: pytest 867 → 870 passed/11 skipped/0 failed + coverage 84.85% → 84.91%(>= 70% threshold) + ruff/format/mypy 0 에러 + mobile/web tsc 0 + mobile lint 0. Story 1.x/2.x/3.x/4.1 회귀 0건. Branch story-4-2. Status: review → done.

last_updated: 2026-05-06  # Story 4.2 review — DS 완료(Amelia). `app/adapters/expo_push.py`(ExpoPushAdapter lazy singleton + send_nudge/send_nudges_bulk/fetch_receipts 3 helper + tenacity 3회 backoff retry + ExpoPushError 계층 3 서브클래스 + asyncio.to_thread sync SDK wrap) + `app/workers/scheduler.py`(AsyncIOScheduler timezone='Asia/Seoul' SOT + build_scheduler/start_scheduler/shutdown_scheduler 3 helper + env in {ci,test} disable) + `app/workers/nudge_scheduler.py`(CronTrigger minute='*/30' KST 30분 sweep + 적격 SQL 4 가드 + 자정 cross OR 분기 + DeviceNotRegistered → revoke_push_token Story 4.1 SOT 재사용 + IntegrityError swallow 멱등 가드 + Sentry op='nudge.sweep' transaction + 사용자별 op='nudge.send' child span + structlog user_id u_{8char} 마스킹) + `app/db/models/notification.py:Notification` ORM(__table_args__ SOT + UNIQUE(user_id,kind,kst_date) + CHECK kind IN('unrecorded_meal') + idx_notifications_user_kst_date) + `alembic/0014_create_notifications.py`(0013 chain + 멱등 검증 통과). lifespan 5번째 자원 wire(scheduler init/cleanup 독립 try/except graceful + env in {ci,test} skip). Mobile `setupGlobalPushHandler(router)` + `resolveDeepLinkTarget` (balancenote:// prefix validation + 외부 URL injection 차단) + `_layout.tsx` `useLastNotificationResponse` cold-start hook 1회 wire(identifier deps 중복 차단). Story 4.1 W8 deferred CLOSED(`getLastNotificationResponse` deprecated + `useLastNotificationResponse` migration). 신규 의존성 1건: `exponent-server-sdk>=2.2`(types-requests 미설치 — `requests.*` mypy override 추가). `.env.example` `EXPO_ACCESS_TOKEN=` 코멘트 갱신(secret-rotation §4 cross-link). docs/runbook/push-notifications-dev.md §5 갱신 + §8 *Cron sweep dev 검증* 신규(환경/fixture/직접 호출 SOP/자연 cron/트러블슈팅) + secret-rotation.md §0 표 갱신 + §4 *EXPO_ACCESS_TOKEN 회전* 5단계 신규(신규 token 발급 + Railway secrets 갱신 + 24h overlap + send 도착 검증 + 기존 token revoke + rollback). pytest 843 → 867 passed/11 skipped/0 failed(+24: adapter 7 + nudge_scheduler 10 + notifications table 2 + lifespan 2 + scheduler 3). coverage 84.85%(>= 70% threshold). ruff/format/mypy 0 에러 + mobile/web tsc 0 에러 + mobile lint 0 에러. Story 1.x/2.x/3.x/4.1 회귀 0건. **Spec deviation 1건**: `tests/conftest.py`에서 `settings.environment = "test"` 강제 — APScheduler in-process cron이 pytest 백그라운드 thread를 leak시키지 않도록 `app/main.py:lifespan`이 `app.state.scheduler = None` + 잡 등록 skip 분기로 들어가게 함(NFR pytest 격리 정합). Branch story-4-2. Status: in-progress → review.

# last_updated: 2026-05-06  # Story 4.2 ready-for-dev — Epic 4 두 번째 스토리(epic-4 in-progress 유지). APScheduler nudge 미기록 푸시 + 딥링크. **신규 의존성 1건**: `exponent-server-sdk>=2.2`(maintainer expo-community, latest 2.2.0). **신규 컴포넌트 5종**: (1) `app/adapters/expo_push.py`(`ExpoPushAdapter` lazy singleton + `send_nudge`/`send_nudges_bulk`/`fetch_receipts` 3 helper + tenacity 3회 backoff retry + `ExpoPushError` 계층 3 서브클래스 — adapter boundary translation Story 3.6 패턴 정합). (2) `app/workers/scheduler.py`(`AsyncIOScheduler(timezone="Asia/Seoul")` SOT + `build_scheduler`/`start_scheduler`/`shutdown_scheduler` 3 helper + env in {"ci","test"} disable). (3) `app/workers/nudge_scheduler.py`(`CronTrigger(minute="*/30", timezone="Asia/Seoul")` 30분 sweep + 적격 사용자 SQL 4 가드(`notifications_enabled=true` AND `expo_push_token IS NOT NULL` AND `deleted_at IS NULL` AND 당일 KST `meals` 0건 AND 동일 날 동일 kind `notifications` 행 미존재) + `notification_time + 30min`이 sweep cycle 30분 윈도우 매칭 + 자정 cross 처리 + DeviceNotRegistered 자동 `revoke_push_token` 호출(Story 4.1 SOT 재사용) + `NUDGE_TITLE`/`NUDGE_BODY` module-level 카피 SOT + Sentry `op="nudge.sweep"` + 사용자별 `op="nudge.send"` child span). (4) `app/db/models/notification.py:Notification` ORM(컬럼 8 + CHECK `kind IN ('unrecorded_meal')` + UNIQUE `(user_id, kind, kst_date)` 멱등 가드 SOT + `idx_notifications_user_kst_date`). (5) `alembic/versions/0014_create_notifications.py`(0013 chain). **Mobile**: `mobile/lib/push.ts:setupGlobalPushHandler(router)` 신규 export — `setNotificationHandler` + `addNotificationResponseReceivedListener`(foreground/background tap → `data.url` 검증 후 `router.push("/(tabs)/meals/input")`). `mobile/app/_layout.tsx`에 `useLastNotificationResponse()` cold start 1회 wire(Story 4.1 W8 deprecated `getLastNotificationResponse` 흡수 — deferred-work CLOSED). **Push payload SOT**: `data.kind="unrecorded_meal"` + `data.url="balancenote://meals/input"` (백엔드/모바일 양방향 정합). **lifespan 5번째 자원**: scheduler init/shutdown 독립 try/except graceful — Story 3.3 4 자원(checkpointer/pool/graph/analysis_service) + Story 3.8 LangSmith init과 동일 패턴, env in {"ci","test"} 시 disable. **NFR-S5 마스킹**: push payload(title/body/data) PII 미포함 + `expo_push_token`은 PII 분류 외(Story 4.1 정합 — Sentry/LangSmith 룰셋 변경 X) + structlog `nudge.send` 이벤트 `user_id`는 `u_{8char}` 마스킹. 단위 테스트 신규 ~22건(adapter 6 + scheduler 2 + nudge_scheduler 10 + notifications table 2 + lifespan 2). pytest 843 → ~865 passed/11 skipped/0 failed + coverage ≥84% 유지. 회귀 0건 가드 — Story 1.x/2.x/3.x/4.1 기존 모든 테스트 통과 의무. .env.example `EXPO_ACCESS_TOKEN=` 주석 형태 추가(Settings 변경 X — Story 1.1 declaration 그대로). docs/runbook/push-notifications-dev.md §5 갱신 + §8 *Cron sweep dev 검증* 신규 + secret-rotation.md §4 *EXPO_ACCESS_TOKEN 회전 5단계* 신규. Branch story-4-2(master에서 분기 완료). Status: backlog → ready-for-dev.

last_updated: 2026-05-06  # Story 4.1 review — DS 완료(Amelia). `users` 4컬럼 + `ck_users_notification_time_kst_format` CHECK + `ux_users_expo_push_token_active` partial UNIQUE index (alembic 0013). `notification_service.py` 4 helper(get_settings/update_settings partial PATCH/register_push_token user-swap atomic/revoke_push_token idempotent). `/v1/notifications` 라우터 4 endpoint(GET 인증만 / PATCH·POST·DELETE require_basic_consents) + 도메인 예외 2건(`NotificationTimeInvalidFormatError` / `NotificationTokenInvalidError`). Mobile expo-notifications + expo-device 의존성 추가 + `push.ts` 4 export + `useNotificationPermission` hook(iOS `provisional` → `granted` 매핑) + `PermissionDeniedView` (features/notifications/ 별 폴더 분리 — kind 분기 X) + `(tabs)/settings/notifications.tsx` UI(Switch + 시·5분 picker + 권한 OFF 배너 + denied modal) + `(tabs)/_layout.tsx` first-time prompt useEffect(`@balancenote/push_prompted` flag) + `auth.tsx:signOut`에 `unregisterPushTokenAsync` wire + `docs/runbook/push-notifications-dev.md` 신규(7섹션). **AC1 CHECK 제약 spec deviation**: 원본 regex `^[0-2][0-9]:[0-5][0-9]:00$`가 SQLAlchemy text parser와 충돌(`:00` named bind / `$` numeric paramstyle 위치 placeholder 해석)해 `EXTRACT(SECOND FROM notification_time) = 0` 의미 동등 표현으로 변경(0초·소수 0 통과, 분초 자유 입력 차단 — 동일 부차 효과). **Mobile jest 인프라 부재**로 mobile 단위 테스트 ~12건은 deferred(Story 8.4 polish forward) — baseline tsc + lint 게이트 정합. pytest 827 → 843 passed/11 skipped/0 failed(+16: db 2 + service 8 + router 6). coverage 84.59% → 84.90%. ruff/format/mypy 0 에러 + mobile/web tsc 0 에러. Story 1.x/2.x/3.x 회귀 0건. Branch story-4-1. Status: in-progress → review.

last_updated: 2026-05-06  # Story 4.1 ready-for-dev — Epic 4 첫 스토리 (epic-4 backlog → in-progress 자동 전환). 알림 설정 ON/OFF + 시간 변경 + 푸시 권한 흐름 + Expo push token 등록. `users` 테이블에 4 컬럼 추가(0013 마이그레이션 — `notifications_enabled` BOOLEAN NOT NULL DEFAULT true / `notification_time` TIME NOT NULL DEFAULT '20:00:00' / `notification_timezone` TEXT NOT NULL DEFAULT 'Asia/Seoul' / `expo_push_token` TEXT nullable + UNIQUE partial index). `app/api/v1/notifications.py` 신규(`GET /v1/notifications/settings` + `PATCH /v1/notifications/settings` + `POST /v1/notifications/devices` + `DELETE /v1/notifications/devices` 4 endpoint, `Depends(require_basic_consents)` wire). `app/services/notification_service.py` 신규(설정 조회/갱신 + token upsert/revoke + NFR-S5 마스킹 정합). `app/db/models/user.py` 4 컬럼 ORM mapping 추가 + DB CHECK(`ck_users_notification_time_kst_format` `notification_time::text ~ '^[0-2][0-9]:[0-5][0-9]:00$'`). `mobile/lib/permissions.ts` `useNotificationPermission` hook 추가(`expo-notifications` `getPermissionsAsync`/`requestPermissionsAsync` wrap, iOS `'provisional'` → `'granted'` 매핑 분기 추가 (Story 2.2 `'limited'` 매핑 패턴 정합)). `mobile/features/notifications/PermissionDeniedView.tsx` 신규(Story 2.2 PermissionDeniedView 패턴 복제 — `kind: 'notifications'` 단독 컴포넌트, "Story 4.1 추가만으로 collation" 메모 정합). `mobile/lib/push.ts` 신규(getExpoPushTokenAsync + `expo-constants` projectId + Android 채널 setNotificationChannelAsync + 등록 실패 graceful + AsyncStorage cache). `mobile/app/(tabs)/settings/notifications.tsx` 신규(ON/OFF Switch + 시간 변경 TimePicker + 권한 OFF 배너 "알림 허용 안 됨 — 설정 열기" + `Linking.openSettings()`). `mobile/features/notifications/api.ts` 신규(useNotificationSettingsQuery + useUpdateNotificationSettingsMutation + useRegisterPushTokenMutation TanStack Query). 신규 Python 의존성 0건 — `exponent_server_sdk` Python SDK는 Story 4.2 시점 추가. 신규 모바일 의존성 `expo-notifications ~0.32` + `expo-device ~8.0`. permission 요청 시점은 *`(tabs)/index.tsx` 첫 진입 시점* (AsyncStorage `@balancenote/push_prompted` flag 1회 — Story 1.6 `tutorial-seen` 패턴 정합) + 사용자가 *알림 설정* 화면 ON Switch 토글 시 회복 prompt. 단위 테스트 신규 ~28건(notification_service 8 + notifications router 6 + user model migration 2 + permissions hook 4 + push token register 4 + notifications.tsx Switch interaction 4). pytest 827 → ~855 passed/11 skipped/0 failed + coverage ≥84% 유지. 회귀 0건 가드 — Story 1.x/2.x/3.x 기존 테스트 모두 통과. Branch story-4-1. Status: backlog → ready-for-dev.
last_updated: 2026-05-05  # Story 3.9 review — DS 완료(Amelia). 23 NOW-ACTIONABLE → 16 AC + 10 Task 일괄 처리. A. 인프라 토폴로지(api/app/core/proxy.py 신규 + 22 Cloudflare CIDR + get_real_client_ip SOT + 5 callsite swap + docs/runbook/deployment.md + cloudflare-ip-refresh.md) — DF111/W2/W10/W1/W17 CLOSED. B. 약관 major/minor 분기(LegalDocument.version_type Literal + CURRENT_VERSION_TYPES SOT + require_basic_consents 분기 + X-Consent-Update-Available 헤더 + privacy/sensitive_personal_info만 minor) — W4 CLOSED. C. Idempotency-Key 30일 TTL(scripts/cleanup_idempotency_keys.py + GHA daily cron) — DF50 CLOSED. D. Vision 비용·캐시·결정성(cache:llm:vision:{sha256} Redis 24h + cost:vision:daily:{YYYY-MM-DD} KST cap + gpt-4o-mini downgrade fallback + ParsedMeal.model_used metadata) — DF4/DF5/DF6 CLOSED. E. 식약처 데이터 보강(MFDS_MAX_PAGES 100→500 + DB_GRP_CM_GROUPS SOT + 외식 50종 CSV 수동 보강) — DF102/DF103 CLOSED. F. Self-RAG/LangGraph(aresume sanitize layer + _node_wrapper deterministic 옵션 + evaluate_retrieval_quality/fetch_user_profile 결정적 노드) — DF89/DF99/DF91 CLOSED. G. SSE/LLM telemetry/Citation(perf marker + _resolve_used_llm 헬퍼 + Literal 확장 + citation regex fullwidth/whitespace 변형 수용) — DF115/DF100/DF104 CLOSED. H. Test infra(conftest make_llm_adapter_mocks SOT + 4 모듈 위임) — DF101 CLOSED. I. 모바일/백엔드 회귀 가드 + 단건 endpoint(MealMacros le=10000 + MealCard time flexShrink:0 + buster SOP + GET /v1/meals/{meal_id} + 모바일 useMealQuery + Vision 환각률 perf marker) — DF28/DF38/DF21/DF18/DF26/DF9 CLOSED. 신규 단위 테스트 ~50건(776 → 824). pytest 824 passed/11 skipped/0 failed + coverage 84.92% + ruff/format/mypy 0 + mobile tsc 0. Story 3.3-3.8 회귀 0건. Branch story-3-9. Status: in-progress → review.

last_updated: 2026-05-06  # Story 3.9 done — CR 3-layer adversarial(Blind Hunter / Edge Case Hunter / Acceptance Auditor) 67 raw findings → 24 patch + 2 defer + 17 dismiss + 3 decision auto-resolved. **핵심 P1**(보안): get_real_client_ip CF-Connecting-IP source 검증(_is_trusted_proxy_ip 통과 시에만 trust) — Railway origin URL 직접 호출 시 CF 헤더 spoofing으로 PIPA audit IP / rate-limit / refresh_token IP 변조 가능했던 회귀 차단. **P2**(비용): Vision cost cap TOCTOU race — INCR-first atomic charge + 실패 시 refund 로직(_adjust_daily_cost SOT, primary→fallback 차액 refund / fallback cap 초과 시 전액 refund + raise / API 호출 실패 시 charged_cost refund). 동시 호출 race로 cap 우회 가능했던 회귀 차단. **P3**(정합): aresume sanitize layer no-op 수정 — _AURESUME_WHITELIST 필터 결과를 실제 사용(사용자 parsed_items propagate, 기존 ``_ = {...}`` discard로 spec contract 위반 회귀 차단) + module-level frozenset 승격 + clarified isinstance str 검증 추가. **P5/P10/P11/P12/P13/P14/P17/P20**(나머지 핵심): meals_images model=parsed.model_used / mobile useMealQuery 빈 string 가드 + encodeURIComponent / _call_vision_fallback @retry decorator / _kst_today_string 1회 계산 동기화 / _redis_fixed_window_incr IP 부재 시 skip(unknown bucket DoS 차단) / cache deserialize 실패 시 redis.delete invalidate / X-Consent-Update-Available privacy/sensitive_personal_info dedupe / WAF Rule 1 모바일 차단 회피 doc fix. **P7/P8/P21/P23**(완성도): cleanup script 실패 path 사후 completed 로그 제거 + ttl_days <= 0 ValueError + MealMacros.macros Optional graceful fallback(legacy 환각 데이터 row >10000 ValidationError 차단) + deferred-work CLOSED 16 AC → 20 AC typo. 신규 테스트 3건(P1 spoof rejection / P4 link-local trust / P8 negative ttl_days) — pytest 824 → 827 passed/11 skipped/0 failed + coverage 84.59%(>= 70% threshold) + ruff/format/mypy 0 + mobile tsc 0. Story 3.3-3.8 회귀 0건. Branch story-3-9. Status: review → done.

last_updated: 2026-05-05  # Story 3.8 done — CR 3-layer adversarial 95 findings → 5 decision-needed + 15 patch + 2 defer + ~70 dismissed. **DN-1 P16**: `_VERSION_KO/EN` doc-key별 dict 분리(privacy만 bump, 4개 미변경 문서 baseline `ko-2026-04-28` 유지 → 기존 사용자 재동의 회귀 차단). **DN-2 P17**: `feedback` whole-mask 폐지 + `text` 키 단독 마스킹(citations/macros/fit_score 영업 데모 가시성 보존). **DN-3 P18**: privacy "처리 위탁(국외)" 재분류 + 거부 권리 Story 5.1/8.4 후속 약속. **DN-4 P19**: dev_smoke env guard(`{dev,test,ci}` 화이트리스트) + Consent synthetic 마커. **DN-5 P20**: init 실패 시 `LANGCHAIN_TRACING_V2=false` 강제(unmasked trace fail-closed). **P1-P15**: langsmith 상한 `<0.9` + private import try/except + Client 부분-실패 시 singleton 리셋 + mask_run_inputs 비-dict/list/tuple 처리 + cycle 가드(id() 추적) + depth limit 32 + LangSmithNotFoundError narrow + CI `pipefail` + 한·영 accuracy regex + measurement-failure PR 코멘트 표시 + env keys cleanup + autouse fixture(test pollution 차단) + vacuous test 3건 교체(진짜 list 재귀 + tracer import 가드) + JSONDecodeError→ValueError + row value type 검증 + whitespace API key exit-1 + structlog `eval.upload.{skipped,dry_run,created,force_append_no_dedupe}` + dashboard count 13→19 + privacy 정확 일치 테스트. test_consents_router._payload() per-doc version 매핑 갱신. DF110-DF111 2 defer(/tmp Linux-only / runbook §1-§7 placeholder). pytest **776 passed/9 skipped/0 failed** + coverage **84.77%** + ruff/format/mypy 0. Story 3.3-3.7 회귀 0건. Branch story-3.8. Status: review → done.
# last_updated: 2026-05-04  # Story 3.8 review — DS 완료(Amelia). app/core/observability.py 신규(LangSmith Client 싱글턴 + mask_run_inputs/outputs + init/get/reset 4 helper + os.environ.setdefault env 동기화 + langsmith.run_trees._CLIENT / _internal._context._GLOBAL_CLIENT process-wide masked client 주입) + sentry SOT 합집합 마스킹(_LANGSMITH_MASKED_KEYS = _SENTRY_MASKED_KEYS | EXTRA{weight_kg/height_cm/allergies/parsed_items/food_items/clarification_options/feedback_text/feedback/meal_text}) + scripts/upload_eval_dataset.py 신규(argparse 5옵션 + idempotent skip + --force append + --dry-run + LANGSMITH_API_KEY 가드) + langsmith>=0.7.31 SDK bump(GHSA-rr7j-v2q5-chgv 보안 패치, lock 0.8.0) + .env.example LANGCHAIN_TRACING_V2=false 디폴트 + PRIVACY_POLICY_KO/EN LangSmith Inc. 운영 옵저버빌리티 단락 + _VERSION_{KO,EN} bump(ko-2026-04-28 → ko-2026-05-04) + docs/observability/langsmith-dashboard.md 신규(공개 보드 4시나리오 + 마스킹 검증 + 트러블슈팅) + docs/runbook/secret-rotation.md 신규(Story 1.1 부재 보강 + LangSmith §3 5단계) + .github/workflows/ci.yml langsmith-eval-regression job 추가(workflow_dispatch + langsmith-eval 라벨 트리거 옵션 + continue-on-error true + PR 코멘트 정확도 표시). LangGraph native tracing은 LANGCHAIN_TRACING_V2 env 1개로 활성 — 라우터(api/v1/analysis.py)/서비스(analysis_service.py)/그래프(pipeline.py)/노드(graph/nodes/*.py) 변경 0(D4 git diff 검증). 단위 테스트 38건 신규(test_observability 19 + test_observability_tracing 4 + test_upload_eval_dataset 9 + test_legal_documents 4 + test_dependency_versions 2). pytest 763 passed/9 skipped/0 failed + coverage 84.74% + ruff/format/mypy 0 에러 + mobile/web tsc 0 에러. Story 3.3-3.7 회귀 0건. DS smoke verification 3 incident 해결(401 env 동기화 / process-wide client wiring / meal_text alias redact) — 실 trace 보드 시각 검증 4회(v1-v4) 후 모든 PII 키 "***" redact 확인. capture_logs() / cache_logger_on_first_use=True 충돌은 autouse fixture(structlog.reset_defaults + observability.log 재바인딩)로 해결. Branch story-3.8. Status: in-progress → review.
# last_updated: 2026-05-04  # Story 3.7 done — CR 3-layer adversarial 100+ findings → 8 decision/26 patch/6 defer/30 dismissed. 26 patch 일괄 적용(Redis pipeline atomic + NaN guard + Sentry transaction + selectinload re-SELECT + mobile race fixes 7건 + AbortController + inactivity timer + clarify multi-tap guard + AC18 4번째 케이스 + 2 perf skeleton). DF110-DF115 6 defer. pytest 725 passed/9 skipped/0 failed + coverage 84.61% + ruff/format/mypy 0. mobile tsc 0. Story 3.3-3.6 회귀 0건. Branch story-3.7 — review → done.
# last_updated: 2026-05-04  # Story 3.6 done — CR 42 patches 일괄 적용(5 BLOCKER 그룹 + 17 MAJOR + 18 MINOR + 7 decision-needed 추천 적용 + 6 defer). BLOCKER 그룹: A(cache hit cited_chunk_indices misalign + ad-regen text/citations divergence + citation-fallback indices=[0] mismatch — chunks fingerprint cache_key + _harden_after_regen helper로 해결) / B1=a(광고 가드 negative lookahead stop-list — 예방접종/처방전/완화병동/진단서/완화제/처방약 등 garbled output 차단, `치료법` cosmetic 수용) / C1+C2(Sentry include_local_variables=False + max_request_body_size=never + attach_stacktrace=False + mask_event 이 exception.values value/stacktrace.frames vars 마스킹 + _MESSAGE_LEAK_PATTERN 정규식 본문 echo 방어) / D1(openai/anthropic permanent error를 adapter boundary에서 LLMRouterUnavailable/PayloadInvalid typed 변환 → router fallback 정상 발동) / E1(dual_exhausted safe-fallback에 ## 평가 + ## 다음 행동 헤더 강제). 17 MAJOR: RetryError dead code 제거(reraise=True 정합) + comment NUL/space 정합 + validate_citations 3-tuple 강제(콤마 ≥2) + cache used_llm Literal validation + Anthropic JSON fence prose 견고화(_extract_json_candidate balanced-brace) + _call_claude multi-block content iterate + tenacity wait math docstring + asyncio.wait_for(25s) outer deadline + httpx subset narrowing(TimeoutException/NetworkError/RemoteProtocolError) + nested-paren citation regex(`(출처: 기관 (2020) 문서)` 지원) + NFC allergies + math.isfinite weight/height guard + MealOCRUnavailableError adapter boundary translate + prompt-injection <user_text>...</user_text> + 500자 cap + KOREAN_MEAL_CONTEXT 정량 비율(25/35/30/10) 제거 + 기관 list 제거 + ValidationError 분리 + capture_exception + 치료법 test rename(known-limitation) + 명사 합성 false-positive 차단 테스트 추가. 18 MINOR: pipeline ordering, published_year=None omit, schema_hint Pydantic list 제거, json.dumps ensure_ascii=False, redis.get/set asyncio.wait_for(2s), adjacent prohibited terms 테스트(진단치료), NFC assertion 모듈 import time, idempotency 테스트, safe-action SOT 단일화, used_llm pre-validate, eval is_regen dead toggle 제거, token_count log, frozen=True FeedbackLLMOutput. Decisions: B1=a(stop-list) / MJ-20=a(cache_key model+chunks fingerprint) / MJ-21=b(정량 제거) / MJ-22=b(outer wait_for 25s) / MJ-23=a(regen instruction 누적 _accumulated_user) / mn-25=b(AC11 whitelist 확장 — violations_replaced_count + reason 추가) / mn-28=a(chunks=[] KDRIs 기본 cite). Defer: DF100~DF105(MJ-24/MJ-25/mn-6/mn-16/mn-30/MJ-19). pytest 666 passed/9 skipped/0 failed + coverage 84.90% + ruff/format/mypy 0. Branch feat/story-3.6. Status: review → done.
# last_updated: 2026-05-03  # Story 3.5 done — CR 19 patches 통합 (3 BLOCKER + 5 MAJOR + 9 MINOR + 2 decision-needed). D-1 quantity multiplier per-100g 통일(`1인분` → 2.5 — `_KOREAN_SERVING_GRAMS_ESTIMATE = 250.0` 한국 외식 1인분 추정) + D-2 `to_summary_label` 어댑터 helper(domain 4-band ↔ presentation 5-band, MealAnalysisSummary wiring 시점에 사용) + B-1 메밀(buckwheat) substring → 밀(wheat) cross-allergen 차단(`_LABEL_SUBSTRING_EXCLUSIONS = {"밀": ("메밀",)}`) + B-2 `넛` alias가 도넛/코코넛에 fire 차단(`_ALIAS_TEXT_EXCLUSIONS = {"넛": ("도넛", "코코넛")}`) + B-3 `기타` 22-라벨 substring 식약처 카테고리 false-positive 차단(`_SKIP_SUBSTRING_LABELS = frozenset({"기타"})` + `ALLERGEN_ALIAS_MAP`에 `"기타알레르기성분" → "기타"` explicit alias 추가) + M-1 `caplog`(structlog 미캡처 vacuous-pass) → `structlog.testing.capture_logs` 마스킹 테스트 + happy-path BMR 계산 경로 마스킹 검증 + `_FORBIDDEN_LOG_KEYS` key-level + value substring fail-safe + M-2 `FoodItem.name = Field(min_length=1)` + `aggregate_meal_macros` early continue + M-3 NaN/Inf 가드 + M-4 kg 분기 (10× per-100g basis) + M-5 vacuous test 단언 보강(`r1.macro != r2.macro or r1.calorie != r2.calorie` lookup 분기 입증) + m-1 alias map 가드 `<=14` → `<=13`(B-3 추가 정합) + m-2 `_coerce_*` masked log warning + m-3 `violated_count` self-emitted 문자열 파싱 → `len(reasons)` 직접 사용 + m-4 BMR ValueError catch → graceful incomplete_data + m-5 mid-range 점수 reason 보강(`<25` 구간 deviation% 명시 + neutral fallback total ≥ 80일 때만) + m-6 BMR 음수 출력 가드 + m-7 NFC 정규화 module init + m-8 legacy compat test docstring 명시(Pydantic default 동작 검증만, band 일관성 미시사) + m-9 clarify→aresume happy path `fit_score > 0` 단언 추가(AC10). 회귀 가드 13건 추가(buckwheat/donut/coconut/walnut food/기타가공식품/기타치킨/기타알레르기성분/quantity_kg/quantity_NaNg/BMR 음수). 6 defer(`sex="female"` Story 8.4 polish, negation phrasing, 포크 비식품, 와인/카제인 alias, dose threshold, BMR ratio sanity) + 11 dismissed. ruff/format/mypy 0 에러, pytest 608 passed/8 skipped/0 failed(+13 새 테스트), coverage 86.69%(>= 70% threshold). Story 3.3/3.4 회귀 0건. Status: review → done.
# last_updated: 2026-05-03  # Story 3.4 done — CR 12 patches 통합 (1 BLOCKER + 7 MAJOR + 4 MINOR). force_llm_parse 필드 + aresume update 주입(parse_meal stale DB 무시 — clarification 무한 루프 차단) + parse_meal `if db_items` truthy(빈 리스트 LLM fallback) + compute_retrieval_confidence expected_count 시그니처(missing implicit 0 보수적) + request_clarification 노드 self-fallback(LLM 영구 실패 시 needs_clarification=True 보장 — UI dead state 차단) + sentinel `len(parsed)==1` 가드(multi-item skip 방지) + request_clarification get_state_field(dict roundtrip 안전) + ClarificationVariants max_length 4→10 완화(env override 약속) + korean_foods_100.json 갈비/콜라보 fix(돼지갈비/덮밥) + _format_vector_literal NaN/Inf ValueError + eval test predicted_path warn 측정 + 2개 vacuous test 명시 contract 검증 + 4개 신규 회귀 가드 테스트(force_llm_parse / DB empty / multi-item missing / sentinel multi-item / LLM 실패 self-fallback / dict roundtrip / compute_confidence missing). ruff/format/mypy 0 에러 + pytest 468 passed/8 skipped(perf+eval+seed)/0 failed + coverage 86.87%. Story 3.3 sentinel/1회 한도/node_errors 가드 보존(회귀 0건). Status: review → done.
project: AI_diet
project_key: NOKEY
tracking_system: file-system
story_location: "{project-root}/_bmad-output/implementation-artifacts"

development_status:
  # Epic 1: 기반 인프라 + 인증 + 온보딩·동의
  epic-1: done
  1-1-프로젝트-부트스트랩: done
  1-2-google-oauth-로그인-jwt: done
  1-3-약관-개인정보-동의: done
  1-4-자동화-의사결정-동의: done
  1-5-건강-프로필-입력: done
  1-6-온보딩-튜토리얼: done
  epic-1-retrospective: optional

  # Epic 2: 식단 입력 + 일별 기록 + 권한 흐름
  epic-2: done
  2-1-식단-텍스트-입력-crud: done
  2-2-식단-사진-입력-권한-흐름: done
  2-3-ocr-vision-추출-확인-카드: done
  2-4-일별-식단-기록-화면: done
  2-5-오프라인-텍스트-큐잉-sync: done
  epic-2-retrospective: optional

  # Epic 3: AI 영양 분석 (Self-RAG + 인용 피드백)
  epic-3: in-progress
  3-1-음식-영양-rag-시드-pgvector: done
  3-2-가이드라인-rag-시드-chunking: done
  3-3-langgraph-6노드-self-rag-saver: done
  3-4-음식명-정규화-매칭-fallback: done
  3-5-fit-score-알고리즘-알레르기-단락: done
  3-6-인용형-피드백-광고-가드-듀얼-llm: done
  3-7-모바일-sse-스트리밍-채팅-ui: done
  3-8-langsmith-옵저버빌리티-평가-데이터셋: done
  3-9-에픽-3-후-polish: done
  epic-3-retrospective: optional

  # Epic 4: 알림(nudge) + Web 주간 리포트
  epic-4: done
  4-1-알림-설정-푸시-권한: done
  4-2-apscheduler-nudge-미기록-푸시: done
  4-3-web-주간-리포트-차트: done
  4-4-인사이트-카드-매크로-목표-조정: done
  epic-4-retrospective: optional

  # Epic 5: 계정 관리 + PIPA 권리
  epic-5: backlog
  5-1-건강-프로필-수정: backlog
  5-2-회원-탈퇴-soft-delete-grace: backlog
  5-3-데이터-내보내기-json-csv: backlog
  epic-5-retrospective: optional

  # Epic 6: 결제/구독 sandbox
  epic-6: backlog
  6-1-정기결제-sandbox-신청: backlog
  6-2-구독-해지-영수증-이용내역: backlog
  6-3-결제-webhook-idempotency-키: backlog
  epic-6-retrospective: optional

  # Epic 7: 관리자 + Audit Trust
  epic-7: backlog
  7-1-관리자-로그인-admin-jwt-가드: backlog
  7-2-관리자-사용자-검색-이력-수정: backlog
  7-3-audit-log-자동-기록-연쇄: backlog
  7-4-self-audit-화면-마스킹-원문-토글: backlog
  epic-7-retrospective: optional

  # Epic 8: 운영·인수인계 패키지 + 영업 자료
  epic-8: backlog
  8-1-sop-4종-작성: backlog
  8-2-faq-5종-영업-답변: backlog
  8-3-readme-agents-인수인계: backlog
  8-4-운영-polish-sentry-swagger-rate-limit: backlog
  8-5-클라우드-배포-hardening-runbook: backlog
  8-6-데모-영상-포트폴리오-영업-패키지: backlog
  epic-8-retrospective: optional
